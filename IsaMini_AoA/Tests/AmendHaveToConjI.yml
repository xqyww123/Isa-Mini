--------------------------------------------------
Initial YAML
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: True ∧ True
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
After Have(...) with body
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: True ∧ True
proof:
  - step id: 1
    thought: irrelevant intermediate
    operation: Have
    statement:
      english: for all x::nat, x = x
      conclusion: \<And>x::nat. x = x
    name: h
    proof:
      - step id: 1.1
        operation: Intro
        fixing variables:
          - x: nat
      - step id: 1.2
        operation: Obvious
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof.
Have type: Have
Have sub_nodes types: ['Intro', 'Obvious']
Have status: success
--------------------------------------------------
After amend Have -> InferenceRule(conjI)
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: True ∧ True
proof:
  - step id: 1
    thought: switch to conjI
    operation: Inference Rule
    rule:
      - conjI: ?P ⟶ ?Q ⟶ ?P ∧ ?Q
    derived subgoals:
      - goal id: 1.1
        goal: True
        proof: empty
      - goal id: 1.2
        goal: True
        proof: empty
Amended type: InferenceRule
Amended sub_nodes types: ['GoalNode', 'GoalNode']
Amended status: success
