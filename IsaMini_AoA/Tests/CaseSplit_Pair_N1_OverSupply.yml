--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - p: 'a × 'b
  - P: 'a × 'b ⇒ bool
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: P p
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
MapCase for `Pair`
--------------------------------------------------
The case-split operation produced a case `Pair` (as follows) that does not match any `case_name` you supplied.
  variables:
    - a: 'a
    - b: 'b
  premises:
    - Pair.prem0: p = (a, b)
  goal: P (a, b)
Is any of your supplied proof intended for this case?
  0. alpha
  1. beta
Answer with its index if so, or with empty `indexes` to leave this case without a proof for now.
--------------------------------------------------
After splice (N==1, over-supply)
--------------------------------------------------
variables:
  - p: 'a × 'b
  - P: 'a × 'b ⇒ bool
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: P p
proof:
  - step id: 1
    thought: case-split on pair; over-supply two bodies
    operation: CaseSplit
    target term: p
    fixing variables:
      - a: 'a
      - b: 'b
    assuming premises:
      - Pair.prem0: p = (a, b)
    notice:
      - The case `beta` is not found in the resulting cases of the case-split and is thus dropped.
  - step id: 2
    operation: Obvious
    Error: Automatic proof fails. The goal is non-trivial and a step-by-step proof is required.
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2` to replace it with a proof.
pending proof goal:
  P p
Interactions fired: 1 (expected 1)
