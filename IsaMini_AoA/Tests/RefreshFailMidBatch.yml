--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - x: int
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: x = x + (1 :: int)
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
is_error: False
--------------------------------------------------
After batched fill (first Have fails, rest cascade-cancel)
--------------------------------------------------
variables:
  - x: int
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: x = x + (1 :: int)
proof:
  - step id: 1
    thought: bad aux (false)
    operation: Have
    statement:
      english: false
      conclusion: (0::int) = (1::int)
    name: bad
    proof:
      - step id: 1.1
        operation: Obvious
        Error: |
          Automatic proof fails on the subgoal:
            False
          The goal is non-trivial and a step-by-step proof is required.
    Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.1` to replace it with a proof.
    pending proof goal:
      (0 :: int) = (1 :: int)
  - step id: 2
    thought: later aux
    operation: Have
    statement:
      english: later
      conclusion: x * x \<ge> 0
    name: later
    proof:
      - step id: 2.1
        operation: Obvious
  - step id: 3
    operation: Obvious
sibling kinds: ['Have', 'Have', 'Obvious']
sibling statuses: ['SUCCESS', 'SUCCESS', 'SUCCESS']
