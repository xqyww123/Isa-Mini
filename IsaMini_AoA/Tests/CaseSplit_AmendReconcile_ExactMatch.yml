--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - b: bool
  - P: bool ⇒ bool
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: P b
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
After initial fill (True/False)
--------------------------------------------------
variables:
  - b: bool
  - P: bool ⇒ bool
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: P b
proof:
  - step id: 1
    thought: initial case-split (exact names)
    operation: CaseSplit
    target term: b
    cases:
      - goal id: 1.True
        premises:
          - True.prem0: b
        goal: P True
        proof:
          - step id: 1.True.1
            operation: Obvious
            Error: Automatic proof fails. The goal is non-trivial and a step-by-step proof is required.
        Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.True.1` to replace it with a proof.
        pending proof goal:
          P True
      - goal id: 1.False
        premises:
          - False.prem0: ¬ b
        goal: P False
        proof:
          - step id: 1.False.1
            operation: Obvious
            Error: Automatic proof fails. The goal is non-trivial and a step-by-step proof is required.
        Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.False.1` to replace it with a proof.
        pending proof goal:
          P False
--------------------------------------------------
After amend (silent replace)
--------------------------------------------------
variables:
  - b: bool
  - P: bool ⇒ bool
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: P b
proof:
  - step id: 1
    thought: amend: same names, different body
    operation: CaseSplit
    target term: b
    cases:
      - goal id: 1.True
        premises:
          - True.prem0: b
        goal: P True
        proof:
          - step id: 1.True.1
            thought: t-have
            operation: Have
            statement:
              english: trivial
              isabelle: True
            name: h
            proof: empty
        Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.True.2` to provide the proof.
      - goal id: 1.False
        premises:
          - False.prem0: ¬ b
        goal: P False
        proof:
          - step id: 1.False.1
            thought: f-have
            operation: Have
            statement:
              english: trivial
              isabelle: True
            name: h
            proof: empty
        Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.False.2` to provide the proof.
Interactions fired: 0 (expected 0)
