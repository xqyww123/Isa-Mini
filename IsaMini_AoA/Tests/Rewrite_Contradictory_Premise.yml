--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - P: bool
premises:
  - eq: MyConst1 = MyConst2
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: P
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
After Rewrite
--------------------------------------------------
variables:
  - P: bool
premises:
  - eq: MyConst1 = MyConst2
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: P
proof:
  - step id: 1
    thought: Rewrite premise using definitions to derive contradiction
    operation: Rewrite
    using:
      - MyConst1_def: MyConst1 = (2 :: nat)
      - MyConst2_def: MyConst2 = (3 :: nat)
      - system simplifiers
    targets:
      - eq
    resulting premises:
      - premise0: False
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  P
is_error: False
reason: None
