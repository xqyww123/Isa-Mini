--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - m: nat
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: (∃(k :: nat). k = m ∧ k ≤ m) ⟶ m = m
proof:
  - step id: 1
    operation: Intro
    assuming premises:
      - premise0: ∃(k :: nat). k = m ∧ k ≤ m
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  m = m
--------------------------------------------------
After Obtain with two constraints
--------------------------------------------------
variables:
  - m: nat
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: (∃(k :: nat). k = m ∧ k ≤ m) ⟶ m = m
proof:
  - step id: 1
    operation: Intro
    assuming premises:
      - premise0: ∃(k :: nat). k = m ∧ k ≤ m
  - step id: 2
    thought: Unpack the existential with multiple constraints
    operation: Obtain
    variables:
      - k: nat
    constraints:
      - name: k_eq
        english: k equals m
        conclusion: k = m
      - name: k_le
        english: k is at most m
        conclusion: k \<le> m
    proof:
      - step id: 2.1
        operation: Obvious
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `3` to provide the proof for the following goal.
pending proof goal:
  m = m
Unfinished nodes: 1
