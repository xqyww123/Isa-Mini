--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - n: nat
  - prime: nat ⇒ bool
premises:
  - n_ge: (3 :: nat) ≤ n
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: ∃(p :: nat). prime p ∧ p dvd n ∧ (∀m<(p :: nat). ¬ (prime m ∧ m dvd n))
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
After Rewrite
--------------------------------------------------
variables:
  - n: nat
  - prime: nat ⇒ bool
premises:
  - n_ge: (3 :: nat) ≤ n
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: ∃(p :: nat). prime p ∧ p dvd n ∧ (∀m<(p :: nat). ¬ (prime m ∧ m dvd n))
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
FAILURE: Cannot fill a node with id 1
The simplification made no progress.notice:
  - Fact "nat_exists_least_iff[where ?P = ‹\<lambda>p. prime p \<and> (p::nat) dvd n›]" not found, skipped.

