只在 isamini_lab3 中存在：Iso_A5.thy
diff --color -ru '--exclude=*.proof-cache*' '--exclude=__pycache__' '--exclude=*~' '--exclude=.git*' '--exclude=*.pyc' isamini_base/Iso_Ctx.thy isamini_lab3/Iso_Ctx.thy
--- isamini_base/Iso_Ctx.thy	2026-08-06 17:30:56.699498266 +0800
+++ isamini_lab3/Iso_Ctx.thy	2026-08-06 17:28:55.984590162 +0800
@@ -1,14 +1,30 @@
 theory Iso_Ctx
   imports Minilang
 begin
+declare [[iso_engine = "struct_strict"]]
+
 axiomatization AA :: bool and RR5 :: "nat \<Rightarrow> bool"
-ML \<open>writeln "##CTX-2 shared-tree baseline: two adjacent meta-binders in the conclusion"\<close>
+
+ML \<open>writeln "##CTX-1 conv 层直调 iso_atomize_conv，相邻两个元级绑定器"\<close>
+ML \<open>
+let val ctxt0 = \<^context>
+    val t = Syntax.read_prop ctxt0 "\<And>yyy zzz::nat. RR5 yyy \<and> RR5 zzz"
+    val ctxt = Variable.declare_term t ctxt0
+in case Exn.capture (fn () => Phi_Conv.iso_atomize_conv ctxt (Thm.cterm_of ctxt t)) () of
+     Exn.Res th => writeln ("##CTX-1 OK: " ^ Syntax.string_of_term ctxt (Thm.term_of (Thm.rhs_of th)))
+   | Exn.Exn e => writeln ("##CTX-1 EXN: " ^ Runtime.exn_message e)
+end
+\<close>
+
+ML \<open>writeln "##CTX-2 端到端：目标结论就是相邻两个绑定器（走 INIT 的 init_goal）"\<close>
 lemma ctx2: "\<And>yyy zzz::nat. RR5 yyy \<and> RR5 zzz"
   apply (min_script \<open>PRINT SORRY\<close>)
   oops
-ML \<open>writeln "##CTX-3 shared-tree baseline: HAVE with two adjacent meta-binders"\<close>
+
+ML \<open>writeln "##CTX-3 端到端：HAVE 的结论是相邻两个绑定器"\<close>
 lemma ctx3: "AA"
   apply (min_script \<open>HAVE h: "\<And>yyy zzz::nat. RR5 yyy \<and> RR5 zzz" PRINT SORRY SORRY\<close>)
   oops
 ML \<open>writeln "##CTX-END"\<close>
+
 end
diff --color -ru '--exclude=*.proof-cache*' '--exclude=__pycache__' '--exclude=*~' '--exclude=.git*' '--exclude=*.pyc' isamini_base/Iso_E2E2.thy isamini_lab3/Iso_E2E2.thy
--- isamini_base/Iso_E2E2.thy	2026-08-06 16:37:16.300822765 +0800
+++ isamini_lab3/Iso_E2E2.thy	2026-08-06 16:37:16.300822765 +0800
@@ -1,6 +1,7 @@
 theory Iso_E2E2
   imports Minilang
 begin
+declare [[iso_engine = "struct_tolerant"]]
 
 axiomatization AA :: bool and PP :: "nat \<Rightarrow> bool"
 
diff --color -ru '--exclude=*.proof-cache*' '--exclude=__pycache__' '--exclude=*~' '--exclude=.git*' '--exclude=*.pyc' isamini_base/Iso_E2E.thy isamini_lab3/Iso_E2E.thy
--- isamini_base/Iso_E2E.thy	2026-08-06 16:35:14.689586835 +0800
+++ isamini_lab3/Iso_E2E.thy	2026-08-06 16:35:14.689586835 +0800
@@ -1,6 +1,7 @@
 theory Iso_E2E
   imports Minilang
 begin
+declare [[iso_engine = "struct_tolerant"]]
 
 axiomatization AA :: bool and BB :: bool
            and PP :: "nat \<Rightarrow> bool" and RR5 :: "nat \<Rightarrow> bool"
只在 isamini_lab3 中存在：Iso_Lab.thy
只在 isamini_base 中存在：Iso_NST.thy
只在 isamini_base 中存在：Iso_ObjAtom.thy
只在 isamini_base 中存在：Iso_OldConv.thy
只在 isamini_lab3 中存在：Iso_OL.thy
只在 isamini_lab3 中存在：Iso_Sanity.thy
diff --color -ru '--exclude=*.proof-cache*' '--exclude=__pycache__' '--exclude=*~' '--exclude=.git*' '--exclude=*.pyc' isamini_base/library/aux_thms.ML isamini_lab3/library/aux_thms.ML
--- isamini_base/library/aux_thms.ML	2026-06-11 19:07:13.714556402 +0800
+++ isamini_lab3/library/aux_thms.ML	2026-08-06 16:19:21.027152926 +0800
@@ -34,10 +34,6 @@
 
 end
 
-val iso_atomize : Proof.context -> conv
-val iso_rulify  : Proof.context -> conv
-val iso_atomize': Proof.context -> thm -> thm
-val iso_rulify' : Proof.context -> thm -> thm
 val concl_conv : (Proof.context -> conv) -> Proof.context -> conv
 
 val simp_fact_name' : string -> string
@@ -76,13 +72,6 @@
 val TAG_def      = @{thm TAG_def}
 val TAG_def'sym  = @{thm TAG_def[symmetric]}
 
-val ISO_ALL_def  = @{thm atomize_all[folded ISO_ALL_def, symmetric]}
-val ISO_ALL'sym_def  = @{thm atomize_all[folded ISO_ALL_def]}
-val ISO_IMP_def  = @{thm atomize_imp[folded ISO_IMP_def, symmetric]}
-val ISO_IMP'sym_def  = @{thm atomize_imp[folded ISO_IMP_def]}
-val ISO_PROP_def  = @{thm ISO_PROP}
-val ISO_PROP'sym_def  = @{thm ISO_PROP[symmetric]}
-
 val conjunction_imp = @{thm conjunction_imp}
 val conjunction_imp'sym = @{thm conjunction_imp[symmetric]}
 
@@ -94,43 +83,6 @@
       val y1 = (cv1 ct1)
   in Thm.combination y1 y2 end;
 
-fun iso_atomize ctxt ctm =
-  let open Conv
-   in case Thm.term_of ctm
-   of Const(\<^const_name>\<open>Pure.imp\<close>, _) $ _ $ _ =>
-        ( combination_conv (arg_conv (iso_atomize ctxt))
-                           (iso_atomize ctxt)
-          then_conv rewr_conv Thms.ISO_IMP'sym_def
-        ) ctm
-    | Const(\<^const_name>\<open>Pure.all\<close>, _) $ Abs _ =>
-        ( arg_conv (abs_conv (iso_atomize o snd) ctxt)
-          then_conv rewr_conv Thms.ISO_ALL'sym_def
-        ) ctm
-    | Const(\<^const_name>\<open>Pure.prop\<close>, _) $ _ =>
-        ( rewr_conv Thms.ISO_PROP'sym_def ) ctm
-    | _ => all_conv ctm
-  end
-
-fun iso_rulify ctxt ctm =
-  let open Conv
-   in case Thm.term_of ctm
-   of _ $ (Const(\<^const_name>\<open>ISO_IMP\<close>, _) $ _ $ _) =>
-        ( rewr_conv Thms.ISO_IMP_def then_conv
-          combination_conv (arg_conv (iso_rulify ctxt))
-                           (iso_rulify ctxt) 
-        ) ctm
-    | _ $ (Const(\<^const_name>\<open>ISO_ALL\<close>, _) $ Abs _) =>
-        ( rewr_conv Thms.ISO_ALL_def then_conv
-          arg_conv (abs_conv (iso_rulify o snd) ctxt)
-        ) ctm
-    | _ $ (Const(\<^const_name>\<open>ISO_PROP\<close>, _) $ _) =>
-        ( rewr_conv Thms.ISO_PROP_def ) ctm
-    | _ => all_conv ctm
-  end
-
-val iso_atomize' = Conv.fconv_rule o iso_atomize
-val iso_rulify'  = Conv.fconv_rule o iso_rulify
-
 fun concl_conv C ctxt ctm =
   case Thm.term_of ctm
     of Const(\<^const_name>\<open>Pure.imp\<close>, _) $ _ $ _ =>
只在 isamini_lab3/library 中存在：iso_atomize.ML
diff --color -ru '--exclude=*.proof-cache*' '--exclude=__pycache__' '--exclude=*~' '--exclude=.git*' '--exclude=*.pyc' isamini_base/library/proof.ML isamini_lab3/library/proof.ML
--- isamini_base/library/proof.ML	2026-08-06 12:59:52.043027216 +0800
+++ isamini_lab3/library/proof.ML	2026-08-06 17:05:21.193316151 +0800
@@ -575,7 +575,7 @@
   end
 
 fun init_goal ctxt th = th
-      |> Conv.fconv_rule (concl_conv iso_atomize ctxt)
+      |> Conv.fconv_rule (concl_conv Phi_Conv.iso_atomize_conv ctxt)
       |> protect_goals
 
 val num_goals_of =
@@ -661,7 +661,7 @@
   end
 
 fun finalize_goal ctxt th = th
-      |> Conv.fconv_rule (concl_conv iso_rulify ctxt)
+      |> Conv.fconv_rule (concl_conv Phi_Conv.iso_rulify_conv ctxt)
 
 (* Printer *)
 
@@ -3479,7 +3479,14 @@
             end
 
           val thy01 = Proof_Context.theory_of ctxt01
+          (* The preview items handed to the printer must be in USER shape, not
+             in iso-atomized shape: `goal` has been through `init_goal`
+             (= iso_atomize + protect_goals), and `pretty_tree0` prints items
+             with a bare `Syntax.pretty_term` -- no rulify anywhere on that
+             path.  Rulifying here also restores `&&&`, without which
+             `Logic.dest_conjunctions` cannot split a multi-`shows` HAVE. *)
           val preruns = concl_of (Thm.prop_of goal)
+                  |> (fn t => Thm.term_of (Thm.rhs_of (Phi_Conv.iso_rulify_conv ctxt01 (Thm.cterm_of ctxt01 t))))
                   |> Logic.dest_conjunctions
                   |> filter_out is_term_marker
                   |> map (Skip_Proof.make_thm thy01)
diff --color -ru '--exclude=*.proof-cache*' '--exclude=__pycache__' '--exclude=*~' '--exclude=.git*' '--exclude=*.pyc' isamini_base/Minilang.thy isamini_lab3/Minilang.thy
--- isamini_base/Minilang.thy	2026-08-06 15:27:25.329986773 +0800
+++ isamini_lab3/Minilang.thy	2026-08-06 16:20:17.548994761 +0800
@@ -34,19 +34,55 @@
 definition \<open>TAG X \<equiv> X\<close>
 definition \<open>GOAL (X::prop) \<equiv> X\<close>
 definition \<open>PROTECT X \<equiv> X\<close>
-definition \<open>ISO_ALL \<equiv> HOL.All\<close>
-definition \<open>ISO_IMP \<equiv> HOL.implies\<close>
-definition \<open>ISO_PROP (X::bool) \<equiv> X\<close>
-definition \<open>ISO_TERM (x::'a::{}) \<equiv> True\<close>
-
-lemma ISO_PROP:
-  \<open>Trueprop (ISO_PROP P) \<equiv> Pure.prop (Trueprop P)\<close>
-  unfolding ISO_PROP_def Pure.prop_def .
+
+subsubsection \<open>Isomorphic Atomize (ported from phi-system PLPR)\<close>
+
+ML_file \<open>./library/iso_atomize.ML\<close>
+
+definition \<open>pure_imp_embed \<equiv> (\<longrightarrow>)\<close>
+definition pure_all_embed :: \<open>('a \<Rightarrow> bool) \<Rightarrow> bool\<close> (binder \<open>\<forall>\<^sub>e\<^sub>m\<^sub>b\<^sub>e\<^sub>d \<close> 10)
+  where \<open>pure_all_embed \<equiv> (All)\<close>
+definition \<open>pure_conj_embed \<equiv> (\<and>)\<close>
+definition \<open>pure_prop_embed x \<equiv> x\<close>
+definition \<open>pure_eq_embed \<equiv> (=)\<close>
+definition \<open>pure_term_embed (x::'a::{}) \<equiv> True\<close>
+
+lemma [iso_atomize_rules, symmetric, iso_rulify_rules]:
+  \<open>(X \<equiv> Y) \<equiv> Trueprop (pure_eq_embed X Y)\<close>
+  unfolding pure_eq_embed_def atomize_eq .
+
+lemma [iso_atomize_rules, symmetric, iso_rulify_rules]:
+  \<open>(P \<Longrightarrow> Q) \<equiv> Trueprop (pure_imp_embed P Q)\<close>
+  unfolding atomize_imp pure_imp_embed_def .
+
+lemma [iso_atomize_rules, symmetric, iso_rulify_rules]:
+  \<open>(P &&& Q) \<equiv> Trueprop (pure_conj_embed P Q)\<close>
+  unfolding atomize_conj pure_conj_embed_def .
+
+lemma [iso_atomize_rules, symmetric, iso_rulify_rules]:
+  \<open>(\<And>x. P x) \<equiv> Trueprop (pure_all_embed (\<lambda>x. P x))\<close>
+  unfolding atomize_all pure_all_embed_def .
+
+lemma [iso_atomize_rules, symmetric, iso_rulify_rules]:
+  \<open>PROP Pure.prop (Trueprop P) \<equiv> Trueprop (pure_prop_embed P)\<close>
+  unfolding Pure.prop_def pure_prop_embed_def .
+
+(* phi's 6th predefined rule, `atomize_Ball`, is NOT ported: it is stated over
+   phi's own meta binder `meta_Ball` (+ `Premise`), constants Minilang does not
+   have.  HOL's own `atomize_ball` is deliberately NOT used in its place: its
+   LHS `(\<And>x. x \<in> A \<Longrightarrow> P x)` also matches the atomize_all/atomize_imp rules,
+   which would make the rule set non-confluent. *)
+
+lemma [iso_atomize_rules, symmetric, iso_rulify_rules]:
+  \<open>(TERM (x::'a::{})) \<equiv> Trueprop (pure_term_embed x)\<close>
+  unfolding pure_term_embed_def term_def
+  by (rule equal_intr_rule; (rule TrueI | assumption))
 
 ML_file \<open>./library/aux_thms.ML\<close>
 
-hide_fact ISO_PROP
-hide_const (open) TAG GOAL PROTECT ISO_ALL ISO_IMP ISO_PROP
+hide_const (open) TAG GOAL PROTECT
+  pure_imp_embed pure_all_embed pure_conj_embed pure_prop_embed pure_eq_embed
+  pure_term_embed
 
 ML_file \<open>./library/unify_diagnostic.ML\<close>  (* before aux.ML: xOF uses Unify_Diagnostic *)
 ML_file \<open>./library/aux.ML\<close>
