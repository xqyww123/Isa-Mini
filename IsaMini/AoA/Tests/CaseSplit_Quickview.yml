--------------------------------------------------
Full print (shows variables and premises)
--------------------------------------------------
variables:
  - l: 'a list
  - P: 'a list ⇒ bool
type variables:
  - 'a: type
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: P l
proof:
  - step id: 1
    thought: Case split on list l
    operation: CaseSplit
    target term: l
    cases:
      - goal id: 1.Nil
        premises:
          - Nil.prem0: l = []
        goal: P []
        proof: empty
        Unfinished Proof! Call command `edit` with action `fill` and target step `1.Nil.1` to provide the proof.
      - goal id: 1.Cons
        variables:
          - a: 'a
          - list: 'a list
        premises:
          - Cons.prem0: l = a # list
        goal: P (a # list)
        proof: empty
        Unfinished Proof! Call command `edit` with action `fill` and target step `1.Cons.1` to provide the proof.
--------------------------------------------------
Quickview (should show variables and premises too)
--------------------------------------------------
step 1: CaseSplit l
  - 1.Nil
    premises:
      - Nil.prem0: l = []
    Unfinished Proof. Call command `edit` with action `fill` and target step `1.Nil.1`
    goal: P []
  - 1.Cons
    variables:
      - a: 'a
      - list: 'a list
    premises:
      - Cons.prem0: l = a # list
    Unfinished Proof. Fill step `1.Cons.1`
    goal: P (a # list)
