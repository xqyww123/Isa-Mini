--------------------------------------------------
Initial
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: ∃(f :: nat ⇒ nat). f (Suc (Suc (Suc (Suc (0 :: nat))))) = Suc (Suc (0 :: nat))
proof: empty
Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
After Define (deferred block; residuals LEFT OPEN)
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: ∃(f :: nat ⇒ nat). f (Suc (Suc (Suc (Suc (0 :: nat))))) = Suc (Suc (0 :: nat))
proof:
  - step id: 1
    thought: Define halve; fake flag forces the deferred termination block
    operation: Define
    name: halve
    type: nat ⇒ nat
    equations:
      - halve 0 = 0
      - halve (Suc 0) = 0
      - halve (Suc (Suc n)) = Suc (halve n)
    metric: \<lambda>n::nat. n
    proof of termination:
      - goal id: 1.1
        goal: wf (measure (λ(n :: nat). n))
        proof: empty
        Unfinished Proof! Call command `edit` with action `fill` and target step `1.1.1` to provide the proof.
      - goal id: 1.2
        goal: ∀(n :: nat). (n, Suc (Suc n)) ∈ measure (λ(n1 :: nat). n1)
        proof:
          - step id: 1.2.1
            operation: Intro
            fixing variables:
              - n: nat
        Unfinished Proof! Call command `edit` with action `fill` and target step `1.2.2` to provide the proof for the following goal.
        pending proof goal:
          (n, Suc (Suc n)) ∈ measure (λ(n1 :: nat). n1)
Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  ∃(f :: nat ⇒ nat). f (Suc (Suc (Suc (Suc (0 :: nat))))) = Suc (Suc (0 :: nat))
--------------------------------------------------
After Witness halve (halve USED; residuals still open)
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: ∃(f :: nat ⇒ nat). f (Suc (Suc (Suc (Suc (0 :: nat))))) = Suc (Suc (0 :: nat))
proof:
  - step id: 1
    thought: Define halve; fake flag forces the deferred termination block
    operation: Define
    name: halve
    type: nat ⇒ nat
    equations:
      - halve 0 = 0
      - halve (Suc 0) = 0
      - halve (Suc (Suc n)) = Suc (halve n)
    metric: \<lambda>n::nat. n
    proof of termination:
      - goal id: 1.1
        goal: wf (measure (λ(n :: nat). n))
        proof: empty
        Unfinished Proof! Call command `edit` with action `fill` and target step `1.1.1` to provide the proof.
      - goal id: 1.2
        goal: ∀(n :: nat). (n, Suc (Suc n)) ∈ measure (λ(n1 :: nat). n1)
        proof:
          - step id: 1.2.1
            operation: Intro
            fixing variables:
              - n: nat
        Unfinished Proof! Call command `edit` with action `fill` and target step `1.2.2` to provide the proof for the following goal.
        pending proof goal:
          (n, Suc (Suc n)) ∈ measure (λ(n1 :: nat). n1)
  - step id: 2
    thought: Pick the freshly-defined halve as the witness
    operation: Witness
    witness: halve
Unfinished Proof! Call command `edit` with action `fill` and target step `3` to provide the proof for the following goal.
pending proof goal:
  halve (Suc (Suc (Suc (Suc (0 :: nat))))) = Suc (Suc (0 :: nat))
finished before comment: False
unfinished before comment: ['1.1', '1.2', '<goal>']
witness status before comment: SUCCESS
comment affected: ['1']
--------------------------------------------------
After commenting the Define
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: ∃(f :: nat ⇒ nat). f (Suc (Suc (Suc (Suc (0 :: nat))))) = Suc (Suc (0 :: nat))
proof:
  - step id: 1
    thought: Define halve; fake flag forces the deferred termination block
    operation: Define
    name: halve
    type: nat ⇒ nat
    equations:
      - halve 0 = 0
      - halve (Suc 0) = 0
      - halve (Suc (Suc n)) = Suc (halve n)
    metric: \<lambda>n::nat. n
    (commented out)
  - step id: 2
    thought: Pick the freshly-defined halve as the witness
    operation: Witness
    witness: halve
    Error: This Witness mentions the free variable "halve", which is not declared in scope. This indicates a bug in your witness term: check whether you mistyped the name.
Unfinished Proof! Call command `edit` with action `fill` and target step `3` to provide the proof for the following goal.
pending proof goal:
  ∃(f :: nat ⇒ nat). f (Suc (Suc (Suc (Suc (0 :: nat))))) = Suc (Suc (0 :: nat))
finished after comment: False
unfinished after comment: ['2', '<goal>']
witness status after comment: FAILURE
