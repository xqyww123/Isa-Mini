--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - c: nat
  - a: nat
premises:
  - h1: f a = c
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: g (f a) = c
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
forking_mode: FORKING_WITH_CTXT
fix_verified: True
--------------------------------------------------
Interaction Prompt
--------------------------------------------------
Rule 'my_wrap' (f ?x = g (f ?x)) would cause infinite rewriting.
To use this rule safely, select which specific subterm(s) to rewrite:
  0. f a
Answer with the index(es) of the subterm(s) to rewrite, or leave empty to drop this rule.
fork_triggered: True
--------------------------------------------------
After Rewrite
--------------------------------------------------
variables:
  - c: nat
  - a: nat
premises:
  - h1: f a = c
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: g (f a) = c
proof:
  - step id: 1
    thought: Rewrite using looping rule
    operation: Rewrite
    using:
      - my_wrap: f ?x = g (f ?x)
    targets:
      - the goal
    goal changes into:
      g (g (f a)) = c
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  g (g (f a)) = c
