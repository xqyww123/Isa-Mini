--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - c: nat
  - a: nat
premises:
  - h1: f a = c
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: g (f a) = c
proof: empty
Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
Interaction Prompt
--------------------------------------------------
Rule 'my_wrap' (f ?x = g (f ?x)) would cause infinite rewriting.
To use this rule safely, select which specific subterm to rewrite:
  0. f a
Call `answer_indexes` with the `indexes` of the subterm to rewrite, or with empty `indexes` to drop this rule.
--------------------------------------------------
After Rewrite (via interaction)
--------------------------------------------------
variables:
  - c: nat
  - a: nat
premises:
  - h1: f a = c
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: g (f a) = c
proof:
  - step id: 1
    thought: Rewrite using my_wrap to unfold f into g(f(...))
    operation: Rewrite
    using:
      - my_wrap: f ?x = g (f ?x)
    targets:
      - the goal
    goal changes into:
      g (g (f a)) = c
Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  g (g (f a)) = c
success: False
reason: None
