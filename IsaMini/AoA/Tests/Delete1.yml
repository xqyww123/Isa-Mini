--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - z: int
  - x: int
premises:
  - h1: y = x + (0 :: int)
  - h2: z = y * (1 :: int)
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
variables:
  - y: int
goal: x = z
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
After building 3 steps
--------------------------------------------------
variables:
  - z: int
  - x: int
premises:
  - h1: y = x + (0 :: int)
  - h2: z = y * (1 :: int)
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
variables:
  - y: int
goal: x = z
proof:
  - step id: 1
    thought: helper
    operation: Have
    statement:
      english: x equals y plus 0
      conclusion: x = y
    name: lem1
    proof:
      - step id: 1.1
        operation: Obvious
  - step id: 2
    thought: rewrite
    operation: Rewrite
    using:
      - lem1: x = y
    targets:
      - the goal
    goal changes into:
      y = z
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `3` to provide the proof for the following goal.
pending proof goal:
  y = z
--------------------------------------------------
Overview
--------------------------------------------------
step 1: Have lem1 (proven)
step 2: Rewrite goal
  goal changes into:
    y = z
Error: Unfinished Proof. Call command `edit` with action `fill` and target step `3`
--------------------------------------------------
After deleting step 1 (Have)
--------------------------------------------------
variables:
  - z: int
  - x: int
premises:
  - h1: y = x + (0 :: int)
  - h2: z = y * (1 :: int)
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
variables:
  - y: int
goal: x = z
proof:
  - step id: 2
    thought: rewrite
    operation: Rewrite
    using:
      - Error: fact "lem1" not found
    targets:
      - the goal
    Error: Fact "lem1" not found
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `3` to provide the proof for the following goal.
pending proof goal:
  x = z
--------------------------------------------------
Overview
--------------------------------------------------
step 2: Rewrite goal (failed)
  Error: Fact "lem1" not found
Error: Unfinished Proof. Fill step `3`
goal: x = z
--------------------------------------------------
After inserting Have before step 2
--------------------------------------------------
variables:
  - z: int
  - x: int
premises:
  - h1: y = x + (0 :: int)
  - h2: z = y * (1 :: int)
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
variables:
  - y: int
goal: x = z
proof:
  - step id: 1
    thought: re-add helper
    operation: Have
    statement:
      english: x equals y plus 0
      conclusion: x = y + 0
    name: lem1
    proof: empty
    Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.1` to provide the proof for the following goal.
    pending proof goal:
      x = y + (0 :: int)
  - step id: 2
    thought: rewrite
    operation: Rewrite
    using:
      - lem1: x = y + (0 :: int)
    targets:
      - the goal
    goal changes into:
      y + (0 :: int) = z
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `3` to provide the proof for the following goal.
pending proof goal:
  y + (0 :: int) = z
--------------------------------------------------
Overview
--------------------------------------------------
step 1: Have lem1
  Error: Unfinished Proof. Fill step `1.1`
  goal: x = y + (0 :: int)
step 2: Rewrite goal (changed)
  goal changes into:
    y + (0 :: int) = z
Error: Unfinished Proof. Fill step `3`
