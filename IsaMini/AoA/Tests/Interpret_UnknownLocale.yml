status: FAILURE
reason: Failed to interpret the locale: Undefined locale: "no_such_locale_here"
--------------------------------------------------
After the failed Interpret_Locale
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: (0 :: nat) < (1 :: nat)
proof:
  - step id: 1
    thought: this locale does not exist
    operation: Interpret_Locale
    locale: no_such_locale_here
    qualifier: u
    instantiations:
      - p = 1
    Error: Failed to interpret the locale: Undefined locale: "no_such_locale_here"
Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  (0 :: nat) < (1 :: nat)
--------------------------------------------------
After recovering with Obvious
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: (0 :: nat) < (1 :: nat)
proof:
  - step id: 1
    operation: Obvious
Unfinished nodes: 0
