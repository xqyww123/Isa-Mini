--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - y: int
  - x: int
premises:
  - h1: y = x + (0 :: int)
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: x + (0 :: int) = y
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
Rewrite with discharge: [null] on zero-premise fact
--------------------------------------------------
is_error: True
reason: Cannot fill a node with id 1
OF: the fact has 0 premise(s), but 1 discharge argument(s) were given
the fact is:
  y = x + 0
--------------------------------------------------
Rewrite without discharge (should succeed)
--------------------------------------------------
variables:
  - y: int
  - x: int
premises:
  - h1: y = x + (0 :: int)
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: x + (0 :: int) = y
proof:
  - step id: 1
    thought: Rewrite using h1 without discharge
    operation: Rewrite
    using:
      - h1: y = x + (0 :: int)
      - system simplifiers
    targets:
      - the goal
    goal is solved.
