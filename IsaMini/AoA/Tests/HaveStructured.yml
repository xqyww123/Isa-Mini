--------------------------------------------------
Initial YAML
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: (3 :: nat) < (3 :: nat) + (1 :: nat)
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
After Have with for_any
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: (3 :: nat) < (3 :: nat) + (1 :: nat)
proof:
  - step id: 1
    thought: Prove a general lemma with explicit variable binding
    operation: Have
    statement:
      english: n plus 1 is greater than n for any natural n
      for_any:
        n: nat
      conclusion: n + 1 > n
    name: succ_gt
    proof: empty
    Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.1` to provide the proof for the following goal.
    pending proof goal:
      n < n + (1 :: nat)
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof.
--------------------------------------------------
After proving Have sub-goal
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: (3 :: nat) < (3 :: nat) + (1 :: nat)
proof:
  - step id: 1
    thought: Prove a general lemma with explicit variable binding
    operation: Have
    statement:
      english: n plus 1 is greater than n for any natural n
      for_any:
        n: nat
      conclusion: n + 1 > n
    name: succ_gt
    proof:
      - step id: 1.1
        operation: Obvious
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof.
--------------------------------------------------
After closing outer goal
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: (3 :: nat) < (3 :: nat) + (1 :: nat)
proof:
  - step id: 1
    thought: Prove a general lemma with explicit variable binding
    operation: Have
    statement:
      english: n plus 1 is greater than n for any natural n
      for_any:
        n: nat
      conclusion: n + 1 > n
    name: succ_gt
    proof:
      - step id: 1.1
        operation: Obvious
  - step id: 2
    operation: Obvious
    with facts:
      - succ_gt: ?n < ?n + (1 :: nat)
Unfinished nodes: 0
