--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - b: bool
  - P: bool ⇒ bool
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: P b
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
Interaction Prompt for actual case `True`
--------------------------------------------------
The case-split operation produced a case `True` (as follows) that does not match any `case_name` you supplied.
  premises:
    - True.prem0: b
  goal: P True
Is any of your supplied proof intended for this case?
  0. new-wrong
Call `answer_index` with the `index` if so, or with null to leave this case without a proof for now.
--------------------------------------------------
Interaction Prompt for actual case `False`
--------------------------------------------------
The case-split operation produced a case `False` (as follows) that does not match any `case_name` you supplied.
  premises:
    - False.prem0: ¬ b
  goal: P False
Is any of your supplied proof intended for this case?
  0. new-wrong
Call `answer_index` with the `index` if so, or with null to leave this case without a proof for now.
--------------------------------------------------
After CaseSplit with dropped supplied
--------------------------------------------------
variables:
  - b: bool
  - P: bool ⇒ bool
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: P b
proof:
  - step id: 1
    thought: case-split on boolean b; supplied name does not match any actual case
    operation: CaseSplit
    target term: b
    cases:
      - goal id: 1.True
        premises:
          - True.prem0: b
        goal: P True
        proof: empty
        Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.True.1` to provide the proof.
      - goal id: 1.False
        premises:
          - False.prem0: ¬ b
        goal: P False
        proof: empty
        Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.False.1` to provide the proof.
    notice:
      - The case `wrong` is not found in the resulting cases of the case-split and is thus dropped.
