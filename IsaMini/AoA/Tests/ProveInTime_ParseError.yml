--------------------------------------------------
Initial State
--------------------------------------------------
variables:
  - x: int
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: (0 :: int) ≤ x * x
proof: empty
Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
validate_prove_in_time("if ?a < ?b then abs(?a - ?b) = ?b - ?a"):
  returned: ['Inner syntax error\nFailed to parse prop\n  ↳ parse failed; the offending token is wrapped in 【 】: … - ?b) = ?b - 【?a】']
ascii_of_unicode("if ?a < ?b then ¦?a - ?b¦ = ?b - ?a") = "if ?a < ?b then \<bar>?a - ?b\<bar> = ?b - ?a"
validate_prove_in_time("if ?a < ?b then \<bar>?a - ?b\<bar> = ?b - ?a"):
  returned: ['Inner syntax error\nFailed to parse prop\n  ↳ parse failed; the offending token is wrapped in 【 】: … - ?b\\<bar> = ?b - 【?a】']
_filter_unprovable([ProveInTime("if ?a < ?b then abs(?a - ?b) = ?b - ?a")]): kept=0, warnings=['Ignored "if ?a < ?b then abs(?a - ?b) = ?b - ?a" — not a known Isabelle theorem nor trivially provable. Prove it manually using Have if needed.\nReason: Inner syntax error\nFailed to parse prop\n  ↳ parse failed; the offending token is wrapped in 【 】: … - ?b) = ?b - 【?a】']
_filter_unprovable([ProveInTime(unicode ¦ variant)]): kept=0, warnings=['Ignored "if ?a < ?b then ¦?a - ?b¦ = ?b - ?a" — not a known Isabelle theorem nor trivially provable. Prove it manually using Have if needed.\nReason: Inner syntax error\nFailed to parse prop\n  ↳ parse failed; the offending token is wrapped in 【 】: … - ?b\\<bar> = ?b - 【?a】']
Obvious created (should have failure status)
Step 1 status: success
--------------------------------------------------
After Obvious with bad ProveInTime
--------------------------------------------------
variables:
  - x: int
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: (0 :: int) ≤ x * x
proof:
  - step id: 1
    operation: Obvious
--------------------------------------------------
Overview
--------------------------------------------------
proven
Subsequent fill succeeded (tree not stuck)
--------------------------------------------------
Final State
--------------------------------------------------
variables:
  - x: int
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: (0 :: int) ≤ x * x
proof:
  - step id: 1
    operation: Obvious
  - step id: 2
    operation: Obvious
