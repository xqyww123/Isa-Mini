--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - y: nat
  - q: nat
  - x: nat
premises:
  - h1: x mod q = y mod q
  - h2: coprime q (2 :: nat)
  - h3: int x + int y = z
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
variables:
  - z: int
goal: x mod q = y mod q
proof: empty
Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
Sub-test 1: Type clash
--------------------------------------------------
Is error: True
Reason: Cannot fill a node with id 1
cannot discharge premise 1 of GCD.mult_mod_cancel_left with discharge argument 1 (h1)
the premise is:
  ?n * ?a mod ?m = ?n * ?b mod ?m
the discharge argument is:
  x mod q = y mod q
because type nat ⇒ nat ⇒ bool does not unify with ?'a::{euclidean_ring_cancel,semiring_gcd} ⇒ ?'a::{euclidean_ring_cancel,semiring_gcd} ⇒ bool
the premises of GCD.mult_mod_cancel_left are:
  1. ?n * ?a mod ?m = ?n * ?b mod ?m
  2. coprime ?m ?n
--------------------------------------------------
Final state
--------------------------------------------------
variables:
  - y: nat
  - q: nat
  - x: nat
premises:
  - h1: x mod q = y mod q
  - h2: coprime q (2 :: nat)
  - h3: int x + int y = z
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
variables:
  - z: int
goal: x mod q = y mod q
proof: empty
Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
