--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - d: nat
  - c: nat
  - b: nat
  - a: nat
premises:
  - h1: f a = c
  - h2: f b = d
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: g (f a) + f b = c + d
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
Interaction Prompt
--------------------------------------------------
Rule 'my_wrap' (f ?x = g (f ?x)) would cause infinite rewriting.
To use this rule safely, select which specific subterms to rewrite:
  0. f a
  1. f b
Call `answer_indexes` with the `indexes` of the subterms to rewrite, or with empty `indexes` to drop this rule.
--------------------------------------------------
After Targeted Rewrite
--------------------------------------------------
variables:
  - d: nat
  - c: nat
  - b: nat
  - a: nat
premises:
  - h1: f a = c
  - h2: f b = d
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: g (f a) + f b = c + d
proof:
  - step id: 1
    thought: Rewrite f a using my_wrap, leave f b alone
    operation: Rewrite
    using:
      - my_wrap: f ?x = g (f ?x)
    targets:
      - the goal
    goal changes into:
      g (f a) + g (f b) = c + d
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  g (f a) + g (f b) = c + d
success: False
reason: None
