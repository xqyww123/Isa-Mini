--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - b: bool
  - P: bool ⇒ bool
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: P b
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
UNEXPECTED interaction fired: Interaction_MapCase
UNEXPECTED interaction fired: Interaction_MapCase
is_error: False
--------------------------------------------------
After CaseSplit singleton splice
--------------------------------------------------
variables:
  - b: bool
  - P: bool ⇒ bool
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: P b
proof:
  - step id: 1
    thought: case-split on b with ONE wrong-named body (triggers splice)
    operation: CaseSplit
    target term: b
    cases:
      - goal id: 1.True
        premises:
          - True.prem0: b
        goal: P True
        proof: empty
        Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.True.1` to provide the proof.
      - goal id: 1.False
        premises:
          - False.prem0: ¬ b
        goal: P False
        proof: empty
        Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.False.1` to provide the proof.
Interactions fired: 2 (expected 0 — splice bypasses MapCase)
=== tree walk ===
- Root id='$root' warnings=0 subs=2
  - GlobalEnv id='global' warnings=0 subs=0
  - GoalNode id='' warnings=0 subs=1
    - CaseSplit id='1' warnings=0 subs=2
      - GoalNode id='1.True' warnings=0 subs=0
      - GoalNode id='1.False' warnings=0 subs=0
