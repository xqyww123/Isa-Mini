--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - b: nat ⇒ nat
  - a: nat ⇒ nat
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: ∀n (k :: nat). k ≤ n ∧ k ≤ n + n ⟶ a k dvd b n
proof:
  - step id: 1
    operation: Intro
    fixing variables:
      - n: nat
      - k: nat
    assuming premises:
      - premise0(1): k ≤ n
      - premise0(2): k ≤ n + n
Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  a k dvd b n
--------------------------------------------------
After Intro (premise0 split into premise0(1), premise0(2))
--------------------------------------------------
variables:
  - b: nat ⇒ nat
  - a: nat ⇒ nat
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: ∀n (k :: nat). k ≤ n ∧ k ≤ n + n ⟶ a k dvd b n
proof:
  - step id: 1
    operation: Intro
    fixing variables:
      - n: nat
      - k: nat
    assuming premises:
      - premise0(1): k ≤ n
      - premise0(2): k ≤ n + n
Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  a k dvd b n
--------------------------------------------------
IH-fact picker (multi-thm)
--------------------------------------------------
These in-scope facts mention n and k. Select the ones you will need later in this induction proof. Facts you leave unselected become unusable after the induction! But selecting ones you won't need bloats the induction and makes it harder to work with. Call `answer_indexes` with their indexes; empty = none.
  0. premise0(1): k ≤ n
  1. premise0(2): k ≤ n + n
--------------------------------------------------
After Induction (only the selected indexed theorem carried)
--------------------------------------------------
variables:
  - b: nat ⇒ nat
  - a: nat ⇒ nat
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: ∀n (k :: nat). k ≤ n ∧ k ≤ n + n ⟶ a k dvd b n
proof:
  - step id: 1
    operation: Intro
    fixing variables:
      - n: nat
      - k: nat
    assuming premises:
      - premise0(1): k ≤ n
      - premise0(2): k ≤ n + n
  - step id: 2
    thought: induct on n, generalize k; pick a subset of the multi-thm fact
    operation: Induction
    target term: n :: nat
    generalized variables: k
    IH facts: premise0(1)
    cases:
      - goal id: 2.0
        variables:
          - n: nat
        premises:
          - premise0(1): n ≤ (0 :: nat)
        goal: a n dvd b (0 :: nat)
        proof: empty
        Unfinished Proof! Call command `edit` with action `fill` and target step `2.0.1` to provide the proof.
      - goal id: 2.Suc
        variables:
          - n: nat
          - k1: nat
        premises:
          - premise0(1): k1 ≤ Suc n
          - Suc.IH: ?k ≤ n ⟶ a ?k dvd b n
        goal: a k1 dvd b (Suc n)
        proof: empty
        Unfinished Proof! Call command `edit` with action `fill` and target step `2.Suc.1` to provide the proof.

offered candidates: ['premise0(1)', 'premise0(2)']
carried into IH: ['premise0(1)']
