--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - p: 'a × 'b
  - P: 'a × 'b ⇒ bool
type variables:
  - 'b: type
  - 'a: type
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: P p
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
After seeding Have at step 1
--------------------------------------------------
variables:
  - p: 'a × 'b
  - P: 'a × 'b ⇒ bool
type variables:
  - 'b: type
  - 'a: type
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: P p
proof:
  - step id: 1
    thought: seed
    operation: Have
    statement:
      english: trivial
      conclusion: True
    name: h
    proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof.
--------------------------------------------------
MapCase for `Pair`
--------------------------------------------------
The case-split operation produced a case `Pair` (as follows) that does not match any `case_name` you supplied.
  variables:
    - a: 'a
    - b: 'b
  premises:
    - Pair.prem0: p = (a, b)
  goal: P (a, b)
Is any of your supplied proof intended for this case?
  0. nope
  1. the-existing-proof
Answer with the index if so, or with null to leave this case without a proof for now.
--------------------------------------------------
After insert_before (stub kept existing)
--------------------------------------------------
variables:
  - p: 'a × 'b
  - P: 'a × 'b ⇒ bool
type variables:
  - 'b: type
  - 'a: type
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: P p
proof:
  - step id: 0A
    thought: case-split on pair; agent picks 'keep existing'
    operation: CaseSplit
    target term: p
    fixing variables:
      - a: 'a
      - b: 'b
    type variables:
      - 'b: type
      - 'a: type
    assuming premises:
      - Pair.prem0: p = (a, b)
    notice:
      - The case `nope` is not found in the resulting cases of the case-split and is thus dropped.
  - step id: 1
    thought: seed
    operation: Have
    statement:
      english: trivial
      conclusion: True
    name: h
    proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  P (a, b)
Interactions fired: 1 (expected 1)
