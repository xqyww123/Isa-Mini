--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - P: nat ⇒ bool
premises:
  - h: P (0 :: nat) ∧ Q (0 :: nat)
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
variables:
  - Q: nat ⇒ bool
goal: P (0 :: nat)
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
Parse_Op_List with discharge entry name=None
--------------------------------------------------
rejected: Bad Argument
proof_operations[0] (Derive).rule.discharge[0].name: expected a string, got NoneType
--------------------------------------------------
fill with discharge entry name=None
--------------------------------------------------
gen_single rejected: Bad Argument
<direct>.rule.discharge[0].name: expected a string, got NoneType
