--------------------------------------------------
After SetupRewriting with Rat.of_int in residue (expect REJECT)
--------------------------------------------------
committed: 1, failure: False
failure: None
variables:
  - x: int
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: (0 :: int) ≤ x * x
proof:
  - step id: 1
    thought: rewrite the coercion into the shadow constant
    operation: SetupRewriting
    redex: rat_of_int n
    residue: Rat.of_int n
    conditions: []
    Error: Fail to claim the rewriting rule because: Rat.of_int is a code-generation shadow constant that the of_int_* lemmas do not apply to. Use rat_of_int instead.
    proof: empty
    Error: Evaluation cancelled due to failures above
Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  (0 :: int) ≤ x * x
