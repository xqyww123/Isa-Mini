--------------------------------------------------
main _edit_verdict (parked worker on 1)
--------------------------------------------------
         fill 1.1       -> LOCKED        blocker=1
         fill 1         -> LOCKED        blocker=1
        amend 1.1       -> LOCKED        blocker=1
        amend 1         -> OK            blocker=-
         fill 1.3       -> LOCKED        blocker=1
         fill 2         -> OK            blocker=-
       delete 1.2       -> OK            blocker=-
       delete 1         -> OK            blocker=-
--------------------------------------------------
main fill 1.1 -> LOCKED message
--------------------------------------------------
is_error=True
This step (or a step above/below it) has a sub-agent working on it, so it can't be edited. Delete it first (which ends that sub-agent), then re-add.
--------------------------------------------------
main amend 1.1 -> LOCKED message (names blocker 1)
--------------------------------------------------
is_error=True
Cannot amend step 1.1 because it belongs to the scope of the sub-agent on step 1. If you really need to change it, either call `subagent` on step 1 to resume that sub-agent with your suggestions, or close it with `close_subagent`.
