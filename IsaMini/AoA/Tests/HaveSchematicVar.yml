--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - x: real
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: ¦x¦ = max x (0 :: real) + max (- x) (0 :: real)
proof: empty
Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
edit failure: None
Step 1 status: failure
--------------------------------------------------
After Have with one schematic variable
--------------------------------------------------
variables:
  - x: real
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: ¦x¦ = max x (0 :: real) + max (- x) (0 :: real)
proof:
  - step id: 1
    thought: an identity stated with a schematic variable
    operation: Have
    statement:
      english: for any real x, |x| = max(x,0) + max(-x,0)
      conclusion: \<bar>?x::real\<bar> = max ?x 0 + max (- ?x) 0
    name: one_var
    Error: Fail to claim the intermediate subgoal because: This Have mentions the schematic variable "?x". A Have statement cannot have schematic variables. If it is meant to be universally quantified, declare it in `for_any` instead.
    proof: empty
    Error: Evaluation cancelled due to failures above
Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof.
