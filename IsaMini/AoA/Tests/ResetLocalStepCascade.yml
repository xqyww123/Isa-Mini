--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - l: 'a list
type variables:
  - 'a: type
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: rev (rev l) = l
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
After seed (cases Nil/Cons, each with an Obvious child)
--------------------------------------------------
variables:
  - l: 'a list
type variables:
  - 'a: type
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: rev (rev l) = l
proof:
  - step id: 1
    thought: induct on l
    operation: Induction
    target term: l
    cases:
      - goal id: 1.Nil
        goal: rev (rev []) = []
        proof:
          - step id: 1.Nil.1
            operation: Obvious
      - goal id: 1.Cons
        variables:
          - a: 'a
        premises:
          - Cons.IH: rev (rev l) = l
        goal: True
        proof:
          - step id: 1.Cons.1
            operation: Obvious
            with facts:
              - Cons.IH: rev (rev l) = l
rename cascade verified: parent=1.renamed, child=1.renamed.1
