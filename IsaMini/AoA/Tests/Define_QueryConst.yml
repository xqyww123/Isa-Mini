--------------------------------------------------
After Define
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: ∃(f :: nat ⇒ nat). f (2 :: nat) = (4 :: nat)
proof:
  - step id: 1
    thought: Define doubling function
    operation: Define
    name: double
    type: nat ⇒ nat
    equations:
      - double n = n + n
    proof of termination: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  ∃(f :: nat ⇒ nat). f (2 :: nat) = (4 :: nat)
Query double.simps (lemma): 1 results, warnings=[]
  Found: double.simps
Query double (constant): 1 results, warnings=[]
  Found: double
--------------------------------------------------
After proof complete
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: ∃(f :: nat ⇒ nat). f (2 :: nat) = (4 :: nat)
proof:
  - step id: 1
    thought: Define doubling function
    operation: Define
    name: double
    type: nat ⇒ nat
    equations:
      - double n = n + n
    proof of termination: empty
  - step id: 2
    thought: Use double as witness
    operation: Witness
    witness: double
  - step id: 3
    operation: Obvious
Unfinished nodes: 0
