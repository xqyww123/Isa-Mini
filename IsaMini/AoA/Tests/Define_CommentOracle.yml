close (pre-comment) fill error: None
finished before comment: False
unfinished before comment: ['1.1', '1.2']
comment affected: ['1']
--------------------------------------------------
After commenting the Define (generic goal already closed)
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: ∃(f :: nat ⇒ nat). f (0 :: nat) = f (0 :: nat)
proof:
  - step id: 1
    thought: Define halve; fake flag forces the deferred termination block
    operation: Define
    name: halve
    type: nat ⇒ nat
    equations:
      - halve 0 = 0
      - halve (Suc 0) = 0
      - halve (Suc (Suc n)) = Suc (halve n)
    metric: \<lambda>n::nat. n
    (commented out)
  - step id: 2
    thought: Pick halve as the witness for the generic goal
    operation: Witness
    witness: halve
    Error: This Witness mentions the free variable "halve", which is not declared in scope. This indicates a bug in your witness term: check whether you mistyped the name.
  - step id: 3
    operation: Obvious
    Error: the evaluation is cancelled due to failure of step `2`
Unfinished Proof! Call command `edit` with action `fill` and target step `3` to replace it with a proof.
pending proof goal:
  ∃(f :: nat ⇒ nat). f (0 :: nat) = f (0 :: nat)
finished after comment: False
unfinished after comment: ['2', '3', '<goal>']
