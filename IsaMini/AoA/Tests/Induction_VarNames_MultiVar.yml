--------------------------------------------------
Initial
--------------------------------------------------
variables:
  - l: nat list
  - P: nat list ⇒ bool
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: P l
proof: empty
Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
After Induction with multi-var Cons
--------------------------------------------------
variables:
  - l: nat list
  - P: nat list ⇒ bool
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: P l
proof:
  - step id: 1
    thought: Induction on l with custom Cons vars
    operation: Induction
    target term: l
    cases:
      - goal id: 1.Nil
        goal: P []
        proof:
          - step id: 1.Nil.1
            operation: Obvious
            Error: Automatic proof fails. The goal is non-trivial and a step-by-step proof is required.
        Unfinished Proof! Call command `edit` with action `fill` and target step `1.Nil.1` to replace it with a proof.
        pending proof goal:
          P []
      - goal id: 1.Cons
        variables:
          - y: nat
          - ys: nat list
        premises:
          - Cons.IH: P ys
        goal: P (y # ys)
        proof:
          - step id: 1.Cons.1
            operation: Obvious
            Error: Automatic proof fails. The goal is non-trivial and a step-by-step proof is required.
        Unfinished Proof! Call command `edit` with action `fill` and target step `1.Cons.1` to replace it with a proof.
        pending proof goal:
          P (y # ys)
Cons case_vars: ['y', 'ys']
