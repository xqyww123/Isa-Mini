--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - x: int
You could add global declarations by calling command `edit` with action `fill` and target step `$global.1`
goal: 0 ≤ x * x
proof: empty
Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof for the following goal.
pending proof goal:
  0 ≤ x * x
--------------------------------------------------
Branch
--------------------------------------------------
variables:
  - x: int
You could add global declarations by calling command `edit` with action `fill` and target step `$global.1`
goal: 0 ≤ x * x
proof:
  - step id: 1
    thought: I don't know
    operation: Branch
    cases:
      - goal id: 1.0
        thought: We first show exhaustiveness of the case split
        goal: 0 < x ∨ x < 0 ∨ x = 0
        proof: empty
        Unfinished Proof! Call command `edit` with action `fill` and target step `1.0.1` to provide the proof for the following goal.
        pending proof goal:
          0 < x ∨ x < 0 ∨ x = 0
      - goal id: 1.1
        premises:
          - fact0: 0 < x
        goal: 0 ≤ x * x
        proof: empty
        Unfinished Proof! Call command `edit` with action `fill` and target step `1.1.1` to provide the proof for the following goal.
        pending proof goal:
          0 ≤ x * x
      - goal id: 1.2
        premises:
          - fact1: x < 0
        goal: 0 ≤ x * x
        proof: empty
        Unfinished Proof! Call command `edit` with action `fill` and target step `1.2.1` to provide the proof for the following goal.
        pending proof goal:
          0 ≤ x * x
      - goal id: 1.3
        premises:
          - fact2: x = 0
        goal: 0 ≤ x * x
        proof: empty
        Unfinished Proof! Call command `edit` with action `fill` and target step `1.3.1` to provide the proof for the following goal.
        pending proof goal:
          0 ≤ x * x
