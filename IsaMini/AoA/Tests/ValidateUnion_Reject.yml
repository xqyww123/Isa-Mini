--------------------------------------------------
Initial YAML
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: (1 :: int) = (1 :: int)
proof: empty
Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
InferenceRule.rule = {bogus}  (FactByName | FactByDescription | None)
--------------------------------------------------
rejected: True
message: Bad Argument
`proof_operations[0] (InferenceRule).rule` must be one of `FactByName`, `FactByDescription`, or null
--------------------------------------------------
Obvious.facts[0] = {bogus}  (FactByName | FactByProposition)
--------------------------------------------------
rejected: True
message: Bad Argument
`proof_operations[0] (Obvious).facts[0]` must be one of `FactByName`, `FactByProposition`, or `FactByDescription`
--------------------------------------------------
CaseSplit.rule = {bogus}  (Literal['default'] | FactByName | FactByDescription)
--------------------------------------------------
rejected: True
message: Bad Argument
`proof_operations[0] (CaseSplit).rule` must be one of `default`, `FactByName`, or `FactByDescription`
--------------------------------------------------
valid values still parse (no reject)
--------------------------------------------------
parsed ok: InferenceRule.rule = {name: conjI}  (FactByName matches)
parsed ok: InferenceRule.rule = None  (None member)
parsed ok: CaseSplit.rule = 'default'  (Literal matches)
multi-member-union rejection verified.
