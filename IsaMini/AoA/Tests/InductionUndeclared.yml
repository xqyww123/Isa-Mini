--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - l: 'a list
type variables:
  - 'a: type
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: rev (rev l) = l
proof: empty
Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
undeclared-free outcome.failure: None
--------------------------------------------------
After Induction ghost_xyz (undeclared-free error in tree)
--------------------------------------------------
variables:
  - l: 'a list
type variables:
  - 'a: type
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: rev (rev l) = l
proof:
  - step id: 1
    thought: Induct on a name that was never declared
    operation: Induction
    target term: ghost_xyz
    Error: Induction failed because: This Induction mentions the free variable "ghost_xyz", which is not declared in scope. This indicates a bug in your induction target: check whether you mistyped the name.
Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  rev (rev l) = l
