--------------------------------------------------
worker@leaf2 while the obligations are still open
--------------------------------------------------
goal: (0 :: nat) < (3 :: nat)
proof: empty
Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
major view after registration (facts are in scope here)
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: (0 :: nat) < (2 :: nat) + (3 :: nat)
proof:
  - step id: 1
    thought: two obligations, each delegatable
    operation: Interpret_Locale
    locale: iw_derived
    qualifier: w
    instantiations:
      - p = 2
      - s = 3
    proof:
      - goal id: 1.1
        goal: (0 :: nat) < (2 :: nat)
        proof:
          - step id: 1.1.1
            operation: Obvious
      - goal id: 1.2
        goal: (0 :: nat) < (3 :: nat)
        proof:
          - step id: 1.2.1
            operation: Obvious
Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  premises:
    - w.iw_base1_axioms: iw_base1 (2 :: nat)
    - w.iw_base2_axioms: iw_base2 (3 :: nat)
    - w.iw_bp: (0 :: nat) < (2 :: nat)
    - w.iw_bs: (0 :: nat) < (3 :: nat)
    - w.iw_derived_axioms: iw_derived (2 :: nat) (3 :: nat)
    - w.iw_sum_pos: (0 :: nat) < (2 :: nat) + (3 :: nat)
  goal: (0 :: nat) < (2 :: nat) + (3 :: nat)
Imported facts
  facts_count reported by kernel: None
  premises visible to successors (6):
    - w.iw_base1_axioms
    - w.iw_base2_axioms
    - w.iw_bp
    - w.iw_bs
    - w.iw_derived_axioms
    - w.iw_sum_pos
--------------------------------------------------
worker@leaf1 AFTER registration -- still no w.* facts in scope
--------------------------------------------------
goal: (0 :: nat) < (2 :: nat)
proof:
  - step id: 1
    operation: Obvious
1.1 obligation premises: []
1.2 obligation premises: []
Unfinished nodes: 0
