--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - Q: 'c ⇒ 'a ⇒ bool
  - P: 'b ⇒ 'a ⇒ bool
  - R: 'a ⇒ bool
type variables:
  - 'b: type
  - 'a: type
  - 'c: type
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: ∀(a :: 'a). R a ⟶ (∀(b :: 'b). P b a) = (∀(c :: 'c). Q c a)
proof:
  - step id: 1
    operation: Intro
    fixing variables:
      - a: 'a
    assuming premises:
      - premise0: R a
Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  (∀(b :: 'b). P b a) = (∀(c :: 'c). Q c a)
--------------------------------------------------
Inference Rule
--------------------------------------------------
variables:
  - Q: 'c ⇒ 'a ⇒ bool
  - P: 'b ⇒ 'a ⇒ bool
  - R: 'a ⇒ bool
type variables:
  - 'b: type
  - 'a: type
  - 'c: type
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: ∀(a :: 'a). R a ⟶ (∀(b :: 'b). P b a) = (∀(c :: 'c). Q c a)
proof:
  - step id: 1
    operation: Intro
    fixing variables:
      - a: 'a
    assuming premises:
      - premise0: R a
  - step id: 2
    thought: Destruct equivalence
    operation: Inference Rule
    rule: default
    derived subgoals:
      - goal id: 2.1
        goal: (∀(b :: 'b). P b a) ⟶ (∀(c :: 'c). Q c a)
        proof:
          - step id: 2.1.1
            operation: Intro
            fixing variables:
              - c: 'c
            assuming premises:
              - premise1: ∀(b :: 'b). P b a
        Unfinished Proof! Call command `edit` with action `fill` and target step `2.1.2` to provide the proof for the following goal.
        pending proof goal:
          Q c a
      - goal id: 2.2
        goal: (∀(c :: 'c). Q c a) ⟶ (∀(b :: 'b). P b a)
        proof:
          - step id: 2.2.1
            operation: Intro
            fixing variables:
              - b: 'b
            assuming premises:
              - premise2: ∀(c :: 'c). Q c a
        Unfinished Proof! Call command `edit` with action `fill` and target step `2.2.2` to provide the proof for the following goal.
        pending proof goal:
          P b a
