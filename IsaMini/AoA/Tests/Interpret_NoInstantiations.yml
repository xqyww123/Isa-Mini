--------------------------------------------------
After Interpret_Locale with no `where` clause
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: (0 :: nat) < (2 :: nat)
proof:
  - step id: 1
    thought: no parameters, so no instantiations at all
    operation: Interpret_Locale
    locale: ini_two
    qualifier: ini
    proof:
      - goal id: 1.1
        goal: (1 :: nat) < (2 :: nat)
        proof: empty
        Unfinished Proof! Call command `edit` with action `fill` and target step `1.1.1` to provide the proof.
Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  premises:
    - ini.ini_gt: (1 :: nat) < (2 :: nat)
    - ini.ini_two_axioms: ini_two
    - ini.ini_zero_lt: (0 :: nat) < (2 :: nat)
  goal: (0 :: nat) < (2 :: nat)
--------------------------------------------------
After discharging the obligation
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: (0 :: nat) < (2 :: nat)
proof:
  - step id: 1
    thought: no parameters, so no instantiations at all
    operation: Interpret_Locale
    locale: ini_two
    qualifier: ini
    proof:
      - goal id: 1.1
        goal: (1 :: nat) < (2 :: nat)
        proof:
          - step id: 1.1.1
            operation: Obvious
Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  premises:
    - ini.ini_gt: (1 :: nat) < (2 :: nat)
    - ini.ini_two_axioms: ini_two
    - ini.ini_zero_lt: (0 :: nat) < (2 :: nat)
  goal: (0 :: nat) < (2 :: nat)
Imported facts
  facts_count reported by kernel: None
  premises visible to successors (3):
    - ini.ini_gt
    - ini.ini_two_axioms
    - ini.ini_zero_lt
--------------------------------------------------
After closing the goal with ini.ini_zero_lt
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: (0 :: nat) < (2 :: nat)
proof:
  - step id: 1
    thought: no parameters, so no instantiations at all
    operation: Interpret_Locale
    locale: ini_two
    qualifier: ini
    proof:
      - goal id: 1.1
        goal: (1 :: nat) < (2 :: nat)
        proof:
          - step id: 1.1.1
            operation: Obvious
  - step id: 2
    thought: close with the imported theorem
    operation: Inference Rule
    rule:
      - ini.ini_zero_lt: (0 :: nat) < (2 :: nat)
Unfinished nodes: 0
