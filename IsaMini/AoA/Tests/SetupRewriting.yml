--------------------------------------------------
Initial YAML
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: myg (3 :: nat) = (8 :: nat)
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
After SetupRewriting
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: myg (3 :: nat) = (8 :: nat)
proof:
  - step id: 1
    thought: Derive a simp rule for myg so the outer goal becomes trivial
    operation: SetupRewriting
    redex: myg n
    residue: n + (5::nat)
    conditions: []
    for_any:
      n: nat
    proof: empty
    Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.1` to provide the proof for the following goal.
    pending proof goal:
      myg n = n + (5 :: nat)
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  premises:
    - setup_rewriting__1: myg ?n = ?n + (5 :: nat)
  goal: myg (3 :: nat) = (8 :: nat)
--------------------------------------------------
After proving SetupRewriting sub-goal
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: myg (3 :: nat) = (8 :: nat)
proof:
  - step id: 1
    thought: Derive a simp rule for myg so the outer goal becomes trivial
    operation: SetupRewriting
    redex: myg n
    residue: n + (5::nat)
    conditions: []
    for_any:
      n: nat
    proof:
      - step id: 1.1
        operation: Obvious
        with facts:
          - myg_def: myg ?n = ?n + (5 :: nat)
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  premises:
    - setup_rewriting__1: myg ?n = ?n + (5 :: nat)
  goal: myg (3 :: nat) = (8 :: nat)
--------------------------------------------------
After closing outer goal
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: myg (3 :: nat) = (8 :: nat)
proof:
  - step id: 1
    thought: Derive a simp rule for myg so the outer goal becomes trivial
    operation: SetupRewriting
    redex: myg n
    residue: n + (5::nat)
    conditions: []
    for_any:
      n: nat
    proof:
      - step id: 1.1
        operation: Obvious
        with facts:
          - myg_def: myg ?n = ?n + (5 :: nat)
  - step id: 2
    thought: Close the outer goal using only the system simplifier
    operation: Rewrite
    using:
      - system simplifiers
    targets:
      - the goal
    goal is solved.
Unfinished nodes: 0
