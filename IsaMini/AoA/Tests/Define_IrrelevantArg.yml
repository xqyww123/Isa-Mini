--------------------------------------------------
Initial YAML
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: ∃(f :: nat ⇒ nat ⇒ nat). f (5 :: nat) (3 :: nat) = (8 :: nat)
proof: empty
Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
After Define
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: ∃(f :: nat ⇒ nat ⇒ nat). f (5 :: nat) (3 :: nat) = (8 :: nat)
proof:
  - step id: 1
    thought: Define gadd
    operation: Define
    name: gadd
    type: nat ⇒ nat ⇒ nat
    equations:
      - gadd a 0 = a
      - gadd a (Suc n) = Suc (gadd a n)
Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  ∃(f :: nat ⇒ nat ⇒ nat). f (5 :: nat) (3 :: nat) = (8 :: nat)
--------------------------------------------------
After Witness
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: ∃(f :: nat ⇒ nat ⇒ nat). f (5 :: nat) (3 :: nat) = (8 :: nat)
proof:
  - step id: 1
    thought: Define gadd
    operation: Define
    name: gadd
    type: nat ⇒ nat ⇒ nat
    equations:
      - gadd a 0 = a
      - gadd a (Suc n) = Suc (gadd a n)
  - step id: 2
    thought: Use gadd as witness
    operation: Witness
    witness: gadd
Unfinished Proof! Call command `edit` with action `fill` and target step `3` to provide the proof for the following goal.
pending proof goal:
  gadd (5 :: nat) (3 :: nat) = (8 :: nat)
--------------------------------------------------
After Obvious
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: ∃(f :: nat ⇒ nat ⇒ nat). f (5 :: nat) (3 :: nat) = (8 :: nat)
proof:
  - step id: 1
    thought: Define gadd
    operation: Define
    name: gadd
    type: nat ⇒ nat ⇒ nat
    equations:
      - gadd a 0 = a
      - gadd a (Suc n) = Suc (gadd a n)
  - step id: 2
    thought: Use gadd as witness
    operation: Witness
    witness: gadd
  - step id: 3
    operation: Obvious
Unfinished nodes: 0
