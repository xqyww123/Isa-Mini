--------------------------------------------------
Initial YAML
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: XXX (1 :: int) (2 :: int) = (3 :: int)
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
After Unfold
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: XXX (1 :: int) (2 :: int) = (3 :: int)
proof:
  - step id: 1
    thought: Unfold the goal
    operation: Unfold
    targets:
      - XXX
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  (1 :: int) + (2 :: int) = (3 :: int)
--------------------------------------------------
Interaction Prompt
--------------------------------------------------
Multiple definitions found for XXX:
  0. Test_Unfold1.XXX_def: XXX ?a ?b = ?a + ?b
  1. Test_Unfold1.XXX_alt: XXX ?a ?b = ?b + ?a
Select definitions to use in unfolding. Call `answer` with `indexes`, or the `name` of a definition, or leave empty to skip.
--------------------------------------------------
After Answer
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: XXX (1 :: int) (2 :: int) = (3 :: int)
proof:
  - step id: 1
    thought: Unfold the goal
    operation: Unfold
    targets:
      - XXX
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  (2 :: int) + (1 :: int) = (3 :: int)
Unfinished nodes: 1
