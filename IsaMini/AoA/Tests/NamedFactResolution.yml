--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - n: nat
  - x: real
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: (0 :: real) < x ⟶ ln (x ^ n) = real n * ln x
proof:
  - step id: 1
    operation: Intro
    assuming premises:
      - premise0: (0 :: real) < x
Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  ln (x ^ n) = real n * ln x
RetrieveForProof(empty): empty
RetrieveForProof(statement='(8::nat) = 2 ^ 3'): IsabelleFact_ProveInTime
ChooseDef(name='NF_XXX_def'): ['IsabelleFact_Presented']
ChooseDef(name=full_name NF_XXX_alt): ['IsabelleFact_Presented']
ChooseDef(name='conjI'): ['IsabelleFact_Presented']
  resolved short_name: conjI
ChooseDef(name='xyzzy_nonexistent_thm'): Interaction_BadAnswer as expected
--------------------------------------------------
Done
--------------------------------------------------
