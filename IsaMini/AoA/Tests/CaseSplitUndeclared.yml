--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - l: 'a list
type variables:
  - 'a: type
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: rev (rev l) = l
proof: empty
Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
undeclared-free outcome.failure: None
--------------------------------------------------
After CaseSplit ghost_xyz (undeclared-free error in tree)
--------------------------------------------------
variables:
  - l: 'a list
type variables:
  - 'a: type
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: rev (rev l) = l
proof:
  - step id: 1
    thought: Case-split on a name that was never declared
    operation: CaseSplit
    target term: ghost_xyz
    Error: Case analysis failed because: This CaseSplit mentions the free variable "ghost_xyz", which is not declared in scope. This indicates a bug in your case-split target: check whether you mistyped the name.
Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  rev (rev l) = l
