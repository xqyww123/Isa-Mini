--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - l: 'a list
type variables:
  - 'a: type
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: rev (rev l) = l
proof: empty
Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
Case Split
--------------------------------------------------
variables:
  - l: 'a list
type variables:
  - 'a: type
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: rev (rev l) = l
proof:
  - step id: 1
    thought: Case split
    operation: CaseSplit
    target term: l
    cases:
      - goal id: 1.Nil
        premises:
          - Nil.prem0: l = []
        goal: True
        proof: empty
      - goal id: 1.Cons
        variables:
          - a: 'a
          - list: 'a list
        premises:
          - Cons.prem0: l = a # list
        goal: True
        proof: empty
--------------------------------------------------
Case Split
--------------------------------------------------
variables:
  - l: 'a list
type variables:
  - 'a: type
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: rev (rev l) = l
proof:
  - step id: 1
    thought: Case split
    operation: CaseSplit
    target term: l
    cases:
      - goal id: 1.Nil
        premises:
          - Nil.prem0: l = []
        goal: True
        proof:
          - step id: 1.Nil.1
            thought: Case split
            operation: CaseSplit
            target term: l
            cases:
              - goal id: 1.Nil.1.Nil1
                premises:
                  - Nil1.prem0: l = []
                goal: True
                proof: empty
              - goal id: 1.Nil.1.Cons
                variables:
                  - a: 'a
                  - list: 'a list
                premises:
                  - Cons.prem0: l = a # list
                goal: True
                proof: empty
      - goal id: 1.Cons
        variables:
          - a: 'a
          - list: 'a list
        premises:
          - Cons.prem0: l = a # list
        goal: True
        proof: empty
