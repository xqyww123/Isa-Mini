--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - n: nat
  - x: real
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: (0 :: real) < x ⟶ ln (x ^ n) = real n * ln x
proof:
  - step id: 1
    operation: Intro
    assuming premises:
      - premise0: (0 :: real) < x
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  ln (x ^ n) = real n * ln x
  fetch_facts[0]: IsabelleFact_Presented
  fetch_facts[1]: IsabelleFact_Unfound
  fetch_facts[2]: IsabelleFact_ProveInTime
  fetch_facts[3]: Interaction_RetrieveForProof
Filled node: Obvious, id=2
  - step id: 2
    operation: Obvious
    with facts:
      - (8::nat) = 2^3
      - log_nat_power: (0 :: real) < ?x ⟶ log ?b (?x ^ ?n) = real ?n * log ?b ?x
--------------------------------------------------
After fill
--------------------------------------------------
variables:
  - n: nat
  - x: real
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: (0 :: real) < x ⟶ ln (x ^ n) = real n * ln x
proof:
  - step id: 1
    operation: Intro
    assuming premises:
      - premise0: (0 :: real) < x
  - step id: 2
    operation: Obvious
    with facts:
      - (8::nat) = 2^3
      - log_nat_power: (0 :: real) < ?x ⟶ log ?b (?x ^ ?n) = real ?n * log ?b ?x
