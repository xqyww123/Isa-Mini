--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - x: int
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: (0 :: int) ≤ x * x
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
[1] Have False — open subgoal
--------------------------------------------------
variables:
  - x: int
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: (0 :: int) ≤ x * x
proof:
  - step id: 1
    thought: introduce unprovable subgoal
    operation: Have
    statement:
      english: False
      conclusion: False
    name: absurd
    proof: empty
    Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.1` to provide the proof for the following goal.
    pending proof goal:
      False
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof.
--------------------------------------------------
[2] fill 1.1 with [Have True, Obvious] — Obvious fails and is kept, _is_trivial=False
--------------------------------------------------
variables:
  - x: int
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: (0 :: int) ≤ x * x
proof:
  - step id: 1
    thought: introduce unprovable subgoal
    operation: Have
    statement:
      english: False
      conclusion: False
    name: absurd
    proof:
      - step id: 1.1
        thought: warm-up helper
        operation: Have
        statement:
          english: True
          conclusion: True
        name: warm
        proof:
          - step id: 1.1.1
            operation: Obvious
      - step id: 1.2
        operation: Obvious
        Error: Automatic proof fails. The goal is non-trivial and a step-by-step proof is required.
    Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.2` to replace it with a proof.
    pending proof goal:
      False
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof.
--------------------------------------------------
[3] multi-amend 1.2 → [Have(True), Obvious] — must not be blocked
--------------------------------------------------
Amended step 1.2-1.3.
All proof goals of step 1.1, step 1.2 are now completed.
Outline:
  step 1: Have absurd
    step 1.1: Have warm (proven)
    step 1.2: Have triv (proven)
    step 1.3: Obvious (failed)
      Error: Automatic proof fails. The goal is non-trivial and a step-by-step proof is required.
    Error: Unfinished Proof. Call command `edit` with action `fill` and target step `1.3`
    goal: False
  Error: Unfinished Proof. Fill step `2`
  goal: (0 :: int) ≤ x * x
is_error: False
--------------------------------------------------
Final state
--------------------------------------------------
variables:
  - x: int
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: (0 :: int) ≤ x * x
proof:
  - step id: 1
    thought: introduce unprovable subgoal
    operation: Have
    statement:
      english: False
      conclusion: False
    name: absurd
    proof:
      - step id: 1.1
        thought: warm-up helper
        operation: Have
        statement:
          english: True
          conclusion: True
        name: warm
        proof:
          - step id: 1.1.1
            operation: Obvious
      - step id: 1.2
        thought: trivial truth
        operation: Have
        statement:
          english: True
          conclusion: True
        name: triv
        proof:
          - step id: 1.2.1
            operation: Obvious
      - step id: 1.3
        operation: Obvious
        with facts:
          - triv: True
        Error: Automatic proof fails. The goal is non-trivial and a step-by-step proof is required.
    Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.3` to replace it with a proof.
    pending proof goal:
      False
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof.
