--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - n: nat
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: n + (0 :: nat) = n
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
global.1 has children: True
--------------------------------------------------
After global Have + Obvious
--------------------------------------------------
variables:
  - n: nat
- step id: global.1
thought: Trivial helper
operation: Have
statement:
  english: True
  conclusion: True
name: helper
proof:
  - step id: global.1.1
    operation: Obvious
You can write global declarations by calling command `edit` with action `fill` and target step `global.2`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: n + (0 :: nat) = n
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
Amend committed: True
Amended node status: success
Amended node warnings: 1
--------------------------------------------------
After amend (print)
--------------------------------------------------
variables:
  - n: nat
- step id: global.1
thought: Prove by induction
operation: Have
statement:
  english: for all m, m + 0 = m
  for_any:
    m: nat
  conclusion: m + 0 = m
name: helper
proof:
  - step id: global.1.1
    thought: induct on m
    operation: Induction
    target term: m
    cases:
      - goal id: global.1.1.0
        goal: (0 :: nat) + (0 :: nat) = (0 :: nat)
        proof: empty
        Error: Unfinished Proof! Call command `edit` with action `fill` and target step `global.1.1.0.1` to provide the proof.
      - goal id: global.1.1.Suc
        variables:
          - m: nat
        premises:
          - Suc.IH: m + (0 :: nat) = m
        goal: True
        proof: empty
notice:
  - Due to changes in the proof structure, previous proof step step global.1.1 has been discarded
You can write global declarations by calling command `edit` with action `fill` and target step `global.2`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: n + (0 :: nat) = n
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
After amend (quickview)
--------------------------------------------------
global declarations:
  step global.1: Have helper
    notice:
      - Due to changes in the proof structure, previous proof step step global.1.1 has been discarded
    step global.1.1: Induction m
      - global.1.1.0
        Error: Unfinished Proof. Call command `edit` with action `fill` and target step `global.1.1.0.1`
        goal: (0 :: nat) + (0 :: nat) = (0 :: nat)
      - global.1.1.Suc (proven)
    the statement is quantified over m
proof:
  Error: Unfinished Proof. Fill step `1`
  goal: n + (0 :: nat) = n
