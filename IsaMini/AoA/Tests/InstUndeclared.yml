--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - Q: nat ⇒ bool
premises:
  - h1: P (0 :: nat)
  - h2: ∀(x :: nat). P x ⟶ Q x
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
variables:
  - P: nat ⇒ bool
goal: Q (0 :: nat)
proof: empty
Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
undeclared-free outcome.failure: Cannot fill a node with id 1
This Instantiation mentions the free variable "ghost_xyz", which is not declared in scope. This indicates a bug in your instantiation value: check whether you mistyped the name.
--------------------------------------------------
After Derive x:=ghost_xyz (undeclared-free error in tree)
--------------------------------------------------
variables:
  - Q: nat ⇒ bool
premises:
  - h1: P (0 :: nat)
  - h2: ∀(x :: nat). P x ⟶ Q x
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
variables:
  - P: nat ⇒ bool
goal: Q (0 :: nat)
proof: empty
Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
