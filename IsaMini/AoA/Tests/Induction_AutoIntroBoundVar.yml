--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - x: int
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: (0 :: int) ≤ x * x
proof: empty
Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
After Have (auto-Intro injected before body-leading Induction)
--------------------------------------------------
variables:
  - x: int
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: (0 :: int) ≤ x * x
proof:
  - step id: 1
    thought: two to the power n is positive
    operation: Have
    statement:
      english: 2^n > 0 for every n
      conclusion: \<forall>n::nat. (2::int)^n > 0
    name: pos_pow
    proof:
      - step id: 1.1
        operation: Intro
        fixing variables:
          - n: nat
      - step id: 1.2
        thought: induction on n
        operation: Induction
        target term: n
        cases:
          - goal id: 1.2.0
            goal: (0 :: int) < (2 :: int) ^ (0 :: nat)
            proof: empty
            Unfinished Proof! Call command `edit` with action `fill` and target step `1.2.0.1` to provide the proof.
          - goal id: 1.2.Suc
            variables:
              - n: nat
            premises:
              - Suc.IH: (0 :: int) < (2 :: int) ^ n
            goal: True
            proof: empty
Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof.
