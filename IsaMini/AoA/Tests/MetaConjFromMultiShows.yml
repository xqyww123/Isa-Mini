--------------------------------------------------
Initial State (goal displays with HOL ∧, but is Pure &&&)
--------------------------------------------------
variables:
  - Q: bool
  - P: bool
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: P ∧ Q
proof: empty
Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
Overview
--------------------------------------------------
Unfinished Proof. Call command `edit` with action `fill` and target step `1`
goal: P ∧ Q
--------------------------------------------------
After SplitConjs (expected to FAIL on the Pure &&& goal)
--------------------------------------------------
variables:
  - Q: bool
  - P: bool
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: P ∧ Q
proof:
  - step id: 1
    thought: Goal looks like P ∧ Q, split it into two subgoals
    operation: SplitConjs
    Error: Failed to split conjunctive goal: SPLIT_CONJS: the leading goal is not a conjunction
Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  P ∧ Q
--------------------------------------------------
Overview after failed split
--------------------------------------------------
step 1: SplitConjs (failed)
  Error: Failed to split conjunctive goal: SPLIT_CONJS: the leading goal is not a conjunction
Unfinished Proof. Fill step `2`
Unfinished nodes: 2
