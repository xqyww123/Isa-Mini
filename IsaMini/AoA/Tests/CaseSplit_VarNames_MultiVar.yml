--------------------------------------------------
Initial
--------------------------------------------------
variables:
  - l: nat list
  - P: nat list ⇒ bool
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: P l
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
After CaseSplit with multi-var Cons
--------------------------------------------------
variables:
  - l: nat list
  - P: nat list ⇒ bool
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
goal: P l
proof:
  - step id: 1
    thought: Case split on list l
    operation: CaseSplit
    target term: l
    cases:
      - goal id: 1.Nil
        premises:
          - Nil.prem0: l = []
        goal: P []
        proof:
          - step id: 1.Nil.1
            operation: Obvious
            Error: Automatic proof fails. The goal is non-trivial and a step-by-step proof is required.
        Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.Nil.1` to replace it with a proof.
        pending proof goal:
          P []
      - goal id: 1.Cons
        variables:
          - x: nat
          - xs: nat list
        premises:
          - Cons.prem0: l = x # xs
        goal: P (x # xs)
        proof:
          - step id: 1.Cons.1
            operation: Obvious
            Error: Automatic proof fails. The goal is non-trivial and a step-by-step proof is required.
        Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.Cons.1` to replace it with a proof.
        pending proof goal:
          P (x # xs)
Cons case_vars: ['x', 'xs']
