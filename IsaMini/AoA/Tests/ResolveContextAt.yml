--------------------------------------------------
A. Live open slot (fresh tree)
--------------------------------------------------
context_at('1'): initialized=True goal=x = z
--------------------------------------------------
Tree built: 1 ok, 2 FAILURE, 3 & 4 cancelled
--------------------------------------------------
variables:
  - z: int
  - x: int
premises:
  - h1: y = x + (0 :: int)
  - h2: z = y * (1 :: int)
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`. If you find the background theory is missing any lemmas you need, formalize and prove them here.
variables:
  - y: int
goal: x = z
proof:
  - step id: 1
    thought: ok
    operation: Have
    statement:
      english: x equals y
      conclusion: x = y
    name: lem1
    proof: empty
    Unfinished Proof! Call command `edit` with action `fill` and target step `1.1` to provide the proof for the following goal.
    pending proof goal:
      x = y
  - step id: 2
    thought: intentionally ill-typed
    operation: Have
    statement:
      english: bad
      conclusion: 1 1 1
    name: bad
    Error: |
      Fail to claim the intermediate subgoal because: Type unification failed: No type arity fun :: one
      
      Type error in application: operator not of function type
      
      Operator:  1 :: ??'a
      Operand:   1 :: ??'b
      
      Coercion Inference:
      
      Local coercion insertion on the operator failed:
      No complex coercion from "??'a" to fun
    proof: empty
    Error: Evaluation cancelled due to failures above
  - step id: 3
    thought: cancelled block
    operation: Have
    statement:
      english: x equals z
      conclusion: x = z
    name: lem3
    Error: the evaluation is cancelled due to failure of step `2`
    proof: empty
    Error: Evaluation cancelled due to failures above
  - step id: 4
    operation: Obvious
    Error: cancelled (step `2` failed)
Error: Evaluation cancelled due to failures above
--------------------------------------------------
B. Failed node with live before-state
--------------------------------------------------
context_at('2'): initialized=True goal=x = z
--------------------------------------------------
C. Dead slot (cancelled block's open slot)
--------------------------------------------------
context_at('3.1'): ValueError: Step 3.1 lies in a cancelled or failed region of the proof and has no usable proof state; cannot use it as query context.
--------------------------------------------------
D. Cancelled siblings: node 3 vs node 4
--------------------------------------------------
context_at('3'): initialized=True goal=x = z
context_at('4'): ValueError: Step 4 lies in a cancelled or failed region of the proof and has no usable proof state; cannot use it as query context.
--------------------------------------------------
E. Nonexistent id
--------------------------------------------------
context_at('9'): ValueError: Step 9 not found.
