{
  "type": "object",
  "properties": {
    "target_steps": {
      "type": "array",
      "items": { "type": "string" },
      "description": "List of step IDs to comment or uncomment"
    },
    "action": {
      "type": "string",
      "enum": ["comment", "uncomment"],
      "description": "Whether to comment out or uncomment the specified steps"
    }
  },
  "required": ["target_steps", "action"],
  "additionalProperties": false
}
