{
  "type": "object",
  "properties": {
    "instantiations": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The schematic variable name, e.g. `?k` or `?k.0`"
          },
          "term": {
            "type": "string",
            "description": "The expression to instantiate the variable with: for a term variable (e.g. `?c`), an Isabelle term with all literal numbers annotated with their types; for a type variable (e.g. `?'a`), a well-formed Isabelle type."
          }
        },
        "required": ["variable", "term"],
        "additionalProperties": false
      },
      "description": "Schematic-variable instantiations"
    }
  },
  "required": ["instantiations"],
  "additionalProperties": false
}
