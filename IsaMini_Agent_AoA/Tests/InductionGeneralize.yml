--------------------------------------------------
Initial YAML
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: ∀N (m :: nat). m ≤ N ⟶ m * m ≤ N * N
proof:
  - step id: 1
    operation: Intro
    fixing variables:
      - N: nat
      - m: nat
    assuming premises:
      - premise0: m ≤ N
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  m * m ≤ N * N
--------------------------------------------------
After Induction (check premises in cases)
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: ∀N (m :: nat). m ≤ N ⟶ m * m ≤ N * N
proof:
  - step id: 1
    operation: Intro
    fixing variables:
      - N: nat
      - m: nat
    assuming premises:
      - premise0: m ≤ N
  - step id: 2
    thought: Induction on N with m generalized
    operation: Induction
    target term: N
    generalized variables: m
    cases:
      - goal id: 2.0
        goal: m * m ≤ (0 :: nat) * (0 :: nat)
        proof: empty
        Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2.0.1` to provide the proof for the following goal.
      - goal id: 2.Suc
        premises:
          - Suc.IH: ?m * ?m ≤ N * N
        goal: m * m ≤ Suc (N + (N + N * N))
        proof: empty
        Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2.Suc.1` to provide the proof for the following goal.
