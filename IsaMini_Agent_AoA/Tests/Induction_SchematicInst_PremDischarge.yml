--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - k: int
  - i: int
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: i ≤ k ⟶ (2 :: int) * i ≤ i + k
proof:
  - step id: 1
    operation: Intro
    assuming premises:
      - premise0: i ≤ k
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  (2 :: int) * i ≤ i + k
--------------------------------------------------
After Induction (Prem0 subgoal should be present)
--------------------------------------------------
variables:
  - k: int
  - i: int
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: i ≤ k ⟶ (2 :: int) * i ≤ i + k
proof:
  - step id: 1
    operation: Intro
    assuming premises:
      - premise0: i ≤ k
  - step id: 2
    thought: induct on i; rely on subgoals policy for the consume premise
    operation: Induction
    target term: i
    cases:
      - goal id: 2.base
        goal: i ≤ k
        proof: empty
        Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2.base.1` to provide the proof for the following goal.
      - goal id: 2.step
        goal: (2 :: int) * k ≤ k + k
        proof: empty
        Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2.step.1` to provide the proof for the following goal.
      - goal id: 2.Prem0
        premises:
          - Prem0.prem0: i ≤ k
          - Prem0.prem1: (2 :: int) * i ≤ i + k
        goal: i - (2 :: int) < k
        proof: empty
        Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2.Prem0.1` to provide the proof for the following goal.

[assertion] 2.Prem0 located: GoalNode
--------------------------------------------------
After attempting Obvious on 2.Prem0
--------------------------------------------------
variables:
  - k: int
  - i: int
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: i ≤ k ⟶ (2 :: int) * i ≤ i + k
proof:
  - step id: 1
    operation: Intro
    assuming premises:
      - premise0: i ≤ k
  - step id: 2
    thought: induct on i; rely on subgoals policy for the consume premise
    operation: Induction
    target term: i
    cases:
      - goal id: 2.base
        goal: i ≤ k
        proof: empty
        Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2.base.1` to provide the proof for the following goal.
      - goal id: 2.step
        goal: (2 :: int) * k ≤ k + k
        proof: empty
        Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2.step.1` to provide the proof for the following goal.
      - goal id: 2.Prem0
        premises:
          - Prem0.prem0: i ≤ k
          - Prem0.prem1: (2 :: int) * i ≤ i + k
        goal: i - (2 :: int) < k
        proof:
          - step id: 2.Prem0.1
            operation: Obvious
            with facts:
              - Prem0.prem0: i ≤ k
