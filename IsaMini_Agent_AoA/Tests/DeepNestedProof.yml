--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - x: int
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: (0 :: int) ≤ x * x
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof for the following goal.
--------------------------------------------------
After 3-level nested Have
--------------------------------------------------
variables:
  - x: int
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: (0 :: int) ≤ x * x
proof:
  - step id: 1
    thought: L1
    operation: Have
    statement:
      english: L1
      isabelle: x * x \<ge> 0
    name: L1
    proof:
      - step id: 1.1
        thought: L2
        operation: Have
        statement:
          english: L2
          isabelle: x * x \<ge> 0
        name: L2
        proof:
          - step id: 1.1.1
            thought: L3
            operation: Have
            statement:
              english: L3
              isabelle: x * x \<ge> 0
            name: L3
            proof:
              - step id: 1.1.1.1
                operation: Obvious
        Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.1.2` to provide the proof for the following goal.
        pending proof goal:
          (0 :: int) ≤ x * x
    Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.2` to provide the proof for the following goal.
    pending proof goal:
      (0 :: int) ≤ x * x
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
depth-3 verified: Have->Have->Have->Obvious
