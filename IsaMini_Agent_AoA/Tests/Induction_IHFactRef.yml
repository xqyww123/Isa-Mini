--------------------------------------------------
Initial YAML
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
variables:
  - p: nat
goal: True
proof: empty
--------------------------------------------------
After Have h (auto-Intro at 1.1 fixes n, assumes n < p)
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
variables:
  - p: nat
goal: True
proof:
  - step id: 1
    thought: sub-lemma for strong induction
    operation: Have
    statement:
      english: for every n, n < p implies True
      isabelle: \<And>n::nat. n < p \<Longrightarrow> True
    name: h
    proof:
      - step id: 1.1
        operation: Intro
        fixing variables:
          - n: nat
        assuming premises:
          - premise0: n < p
--------------------------------------------------
After Induction (case 1 produced with hyp `1.IH`)
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
variables:
  - p: nat
goal: True
proof:
  - step id: 1
    thought: sub-lemma for strong induction
    operation: Have
    statement:
      english: for every n, n < p implies True
      isabelle: \<And>n::nat. n < p \<Longrightarrow> True
    name: h
    proof:
      - step id: 1.1
        operation: Intro
        fixing variables:
          - n: nat
        assuming premises:
          - premise0: n < p
      - step id: 1.2
        thought: strong induction on n, generalize n, keep p fixed
        operation: Induction
        target term: n :: nat
        facts to generalize: premise0
        fixing variables:
          - n: nat
        assuming premises:
          - premise0: n < p
          - 1.IH: ∀m<n. m < p ⟶ True

Displayed hyp names under case 1: ['premise0', '1.IH']
--------------------------------------------------
After Obvious facts=[1.IH]
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
variables:
  - p: nat
goal: True
proof:
  - step id: 1
    thought: sub-lemma for strong induction
    operation: Have
    statement:
      english: for every n, n < p implies True
      isabelle: \<And>n::nat. n < p \<Longrightarrow> True
    name: h
    proof:
      - step id: 1.1
        operation: Intro
        fixing variables:
          - n: nat
        assuming premises:
          - premise0: n < p
      - step id: 1.2
        thought: strong induction on n, generalize n, keep p fixed
        operation: Induction
        target term: n :: nat
        facts to generalize: premise0
        fixing variables:
          - n: nat
        assuming premises:
          - premise0: n < p
          - 1.IH: ∀m<n. m < p ⟶ True
      - step id: 1.3
        operation: Obvious
        with facts:
          - 1.IH: ∀m<n. m < p ⟶ True
is_error: False
reason: 
