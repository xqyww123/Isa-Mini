--------------------------------------------------
Initial YAML
--------------------------------------------------
You could add global declarations by calling command `edit` with action `fill` and target step `$global.1`
goal: Test_Unfold1.XXX (1 :: int) (2 :: int) = (3 :: int)
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof for the following goal.
pending proof goal:
  Test_Unfold1.XXX (1 :: int) (2 :: int) = (3 :: int)
--------------------------------------------------
After Unfold
--------------------------------------------------
You could add global declarations by calling command `edit` with action `fill` and target step `$global.1`
goal: Test_Unfold1.XXX (1 :: int) (2 :: int) = (3 :: int)
proof:
  - step id: 1
    thought: Unfold the goal
    operation: Unfold
    targets:
      - XXX
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  (1 :: int) + (2 :: int) = (3 :: int)
--------------------------------------------------
Interaction Prompt
--------------------------------------------------
Multiple definitions found for XXX:
  - 0: Test_Unfold1.XXX ?a ?b = ?a + ?b
  - 1: Test_Unfold1.XXX ?a ?b = ?b + ?a
Select definition(s) to use in unfolding. Call the `answer` tool with indexes or cancel the operation.
--------------------------------------------------
After Answer
--------------------------------------------------
You could add global declarations by calling command `edit` with action `fill` and target step `$global.1`
goal: Test_Unfold1.XXX (1 :: int) (2 :: int) = (3 :: int)
proof:
  - step id: 1
    thought: Unfold the goal
    operation: Unfold
    targets:
      - XXX
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  (2 :: int) + (1 :: int) = (3 :: int)
Unfinished nodes: 1
