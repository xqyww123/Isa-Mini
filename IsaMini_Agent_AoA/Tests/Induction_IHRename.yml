--------------------------------------------------
Initial YAML
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
variables:
  - p: nat
  - i: nat
  - f: nat ⇒ nat
goal: True
proof: empty
--------------------------------------------------
After Have gen
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
variables:
  - p: nat
  - i: nat
  - f: nat ⇒ nat
goal: True
proof:
  - step id: 1
    thought: sub-lemma to induct on
    operation: Have
    statement:
      english: for every n, if n is at most p-2 then f n is less than p
      isabelle: \<And>n::nat. n \<le> p - 2 \<Longrightarrow> f n < p
    name: gen
    proof:
      - step id: 1.1
        operation: Intro
        fixing variables:
          - n: nat
        assuming premises:
          - premise0: n ≤ p - (2 :: nat)
    Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.2` to provide the proof for the following goal.
    pending proof goal:
      f n < p
--------------------------------------------------
After Induction (IH should use `n`, not internal variant)
--------------------------------------------------
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
variables:
  - p: nat
  - i: nat
  - f: nat ⇒ nat
goal: True
proof:
  - step id: 1
    thought: sub-lemma to induct on
    operation: Have
    statement:
      english: for every n, if n is at most p-2 then f n is less than p
      isabelle: \<And>n::nat. n \<le> p - 2 \<Longrightarrow> f n < p
    name: gen
    proof:
      - step id: 1.1
        operation: Intro
        fixing variables:
          - n: nat
        assuming premises:
          - premise0: n ≤ p - (2 :: nat)
      - step id: 1.2
        thought: strong induction on n, generalize n, keep p and f fixed
        operation: Induction
        target term: n :: nat
        facts to generalize: premise0
        fixing variables:
          - n1: nat
        assuming premises:
          - premise0: n1 ≤ p - (2 :: nat)
          - 1.IH: ∀m<n1. m ≤ p - (2 :: nat) ⟶ f m < p
        Error: The variable i appear in the induction context but are not classified in the 'variables' argument. You should indicate whether each is 'arbitrary' (generalized during induction) or 'fixed' (held constant).
        cases: empty
    Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.3` to provide the proof for the following goal.
    pending proof goal:
      f n < p

Display names in `fixing variables`: ['n1']
Offending IH tokens: 0
