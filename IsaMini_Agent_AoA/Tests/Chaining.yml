--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - c: nat
  - a: nat
premises:
  - ab: a = b
  - bc: b ≤ c
You can write global declarations by calling command `edit` with action `fill` and target step `$global.1`
variables:
  - b: nat
goal: a ≤ c
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof for the following goal.
--------------------------------------------------
After Chaining (named)
--------------------------------------------------
variables:
  - c: nat
  - a: nat
premises:
  - ab: a = b
  - bc: b ≤ c
You can write global declarations by calling command `edit` with action `fill` and target step `$global.1`
variables:
  - b: nat
goal: a ≤ c
proof:
  - step id: 1
    operation: Chaining
    from:
      - ab: a = b
      - bc: b ≤ c
    resulting:
      ac: a ≤ c
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  premises:
    - ac: a ≤ c
  goal: a ≤ c
--------------------------------------------------
After Obvious using ac
--------------------------------------------------
variables:
  - c: nat
  - a: nat
premises:
  - ab: a = b
  - bc: b ≤ c
You can write global declarations by calling command `edit` with action `fill` and target step `$global.1`
variables:
  - b: nat
goal: a ≤ c
proof:
  - step id: 1
    operation: Chaining
    from:
      - ab: a = b
      - bc: b ≤ c
    resulting:
      ac: a ≤ c
  - step id: 2
    operation: Obvious
    with facts:
      - ac: a ≤ c
Unfinished nodes: 0
