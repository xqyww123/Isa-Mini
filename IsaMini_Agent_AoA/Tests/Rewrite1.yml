--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - z: 'a
  - x: 'a
premises:
  - h1: y = x + (0 :: 'a)
  - h2: ∃(aAa :: 'a). z = y * (1 :: 'a) + aAa
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
variables:
  - y: 'a
goal: x = z
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof for the following goal.
--------------------------------------------------
After Rewrite
--------------------------------------------------
variables:
  - z: 'a
  - x: 'a
premises:
  - h1: y = x + (0 :: 'a)
  - h2: ∃(aAa :: 'a). z = y * (1 :: 'a) + aAa
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
variables:
  - y: 'a
goal: x = z
proof:
  - step id: 1
    thought: Rewrite the premises to simplify the equations
    operation: Rewrite
    using:
      - h1: y = x + (0 :: 'a)
      - system simplifiers
    targets:
      - h2
    resulting premises:
      - premise0: ∃(aAa :: 'a). z = (x + (0 :: 'a)) * (1 :: 'a) + aAa
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  x = z
--------------------------------------------------
After Rename Fact
--------------------------------------------------
variables:
  - z: 'a
  - x: 'a
premises:
  - h1: y = x + (0 :: 'a)
  - h2: ∃(aAa :: 'a). z = y * (1 :: 'a) + aAa
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
variables:
  - y: 'a
goal: x = z
proof:
  - step id: 1
    thought: Rewrite the premises to simplify the equations
    operation: Rewrite
    using:
      - h1: y = x + (0 :: 'a)
      - system simplifiers
    targets:
      - h2
    resulting premises:
      - my_premise: ∃(aAa :: 'a). z = (x + (0 :: 'a)) * (1 :: 'a) + aAa
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  x = z
