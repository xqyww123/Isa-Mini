--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - Q: bool ⇒ bool
  - b: bool
  - P: bool ⇒ bool
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: P b ∨ Q b
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
After CaseSplit
--------------------------------------------------
variables:
  - Q: bool ⇒ bool
  - b: bool
  - P: bool ⇒ bool
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: P b ∨ Q b
proof:
  - step id: 1
    thought: Case split on boolean b
    operation: CaseSplit
    target term: b
    cases:
      - goal id: 1.True
        premises:
          - True.prem0: b
        goal: P True ∨ Q True
        proof: empty
        Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.True.1` to provide the proof.
      - goal id: 1.False
        premises:
          - False.prem0: ¬ b
        goal: P False ∨ Q False
        proof: empty
        Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.False.1` to provide the proof.
[probe] InferenceRule._state_after_beginning.top_goals() count=2: P True, P b ∨ Q b
[probe] InferenceRule sub_nodes ids: []
--------------------------------------------------
After InferenceRule (expect spurious 2nd sibling-leak subgoal)
--------------------------------------------------
variables:
  - Q: bool ⇒ bool
  - b: bool
  - P: bool ⇒ bool
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: P b ∨ Q b
proof:
  - step id: 1
    thought: Case split on boolean b
    operation: CaseSplit
    target term: b
    cases:
      - goal id: 1.True
        premises:
          - True.prem0: b
        goal: P True ∨ Q True
        proof:
          - step id: 1.True.1
            thought: Apply disjI1 (produces exactly 1 subgoal from disjunction)
            operation: Inference Rule
            rule:
              - disjI1: ?P ⟶ ?P ∨ ?Q
            derived goal:
              P True
        Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.True.2` to provide the proof for the following goal.
        pending proof goal:
          P True
      - goal id: 1.False
        premises:
          - False.prem0: ¬ b
        goal: P False ∨ Q False
        proof: empty
        Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.False.1` to provide the proof.
