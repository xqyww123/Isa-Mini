--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - x: int
You could add global declarations by calling command `edit` with action `fill` and target step `$global.1`
goal: (0 :: int) ≤ x * x
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof for the following goal.
pending proof goal:
  (0 :: int) ≤ x * x
--------------------------------------------------
Branch
--------------------------------------------------
variables:
  - x: int
You could add global declarations by calling command `edit` with action `fill` and target step `$global.1`
goal: (0 :: int) ≤ x * x
proof:
  - step id: 1
    thought: I don't know
    operation: Branch
    cases:
      - goal id: 1.0
        thought: We first show exhaustiveness of the case split
        goal: (0 :: int) < x ∨ x < (0 :: int) ∨ x = (0 :: int)
        proof: empty
        Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.0.1` to provide the proof for the following goal.
        pending proof goal:
          (0 :: int) < x ∨ x < (0 :: int) ∨ x = (0 :: int)
      - goal id: 1.1
        premises:
          - premise0: (0 :: int) < x
        goal: (0 :: int) ≤ x * x
        proof: empty
        Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.1.1` to provide the proof for the following goal.
        pending proof goal:
          (0 :: int) ≤ x * x
      - goal id: 1.2
        premises:
          - premise1: x < (0 :: int)
        goal: (0 :: int) ≤ x * x
        proof: empty
        Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.2.1` to provide the proof for the following goal.
        pending proof goal:
          (0 :: int) ≤ x * x
      - goal id: 1.3
        premises:
          - premise2: x = (0 :: int)
        goal: (0 :: int) ≤ x * x
        proof: empty
        Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.3.1` to provide the proof for the following goal.
        pending proof goal:
          (0 :: int) ≤ x * x
