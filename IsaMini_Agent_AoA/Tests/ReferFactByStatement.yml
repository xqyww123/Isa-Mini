--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - x: int
You could add global declarations by calling command `edit` with action `fill` and target step `$global.1`
goal: (0 :: int) ≤ x * x
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof for the following goal.
pending proof goal:
  (0 :: int) ≤ x * x
Fullname of notI: [FactRef(full_name='HOL.notI', short_name='notI', fact={'refer_by': 'name', 'name': 'notI'}, expression=['(?P \\<longrightarrow> False) \\<longrightarrow> \\<not> ?P'])]
