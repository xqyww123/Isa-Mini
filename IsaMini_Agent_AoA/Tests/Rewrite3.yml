--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - z: int
  - x: int
premises:
  - h1: y = x + (0 :: int)
  - h2: z = y * (1 :: int)
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
variables:
  - y: int
goal: x = z
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
After Have
--------------------------------------------------
variables:
  - z: int
  - x: int
premises:
  - h1: y = x + (0 :: int)
  - h2: z = y * (1 :: int)
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
variables:
  - y: int
goal: x = z
proof:
  - step id: 1
    thought: I don't know
    operation: Have
    statement:
      english: x squared plus 1 is greater than 0
      isabelle: x = y
    name: lem1
    proof:
      - step id: 1.1
        operation: Obvious
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof.
--------------------------------------------------
After Rewrite
--------------------------------------------------
variables:
  - z: int
  - x: int
premises:
  - h1: y = x + (0 :: int)
  - h2: z = y * (1 :: int)
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
variables:
  - y: int
goal: x = z
proof:
  - step id: 1
    thought: I don't know
    operation: Have
    statement:
      english: x squared plus 1 is greater than 0
      isabelle: x = y
    name: lem1
    proof:
      - step id: 1.1
        operation: Obvious
  - step id: 2
    thought: Rewrite the premises to simplify the equations
    operation: Rewrite
    using:
      - lem1: x = y
    targets:
      - the goal
    goal changes into:
      y = z
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `3` to provide the proof for the following goal.
pending proof goal:
  y = z
--------------------------------------------------
After Amend Have
--------------------------------------------------
variables:
  - z: int
  - x: int
premises:
  - h1: y = x + (0 :: int)
  - h2: z = y * (1 :: int)
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
variables:
  - y: int
goal: x = z
proof:
  - step id: 1
    thought: I don't know!!!
    operation: Have
    statement:
      english: x squared plus 1 is greater than 0
      isabelle: x = y * 1
    name: lem1
    proof:
      - step id: 1.1
        operation: Obvious
  - step id: 2
    thought: Rewrite the premises to simplify the equations
    operation: Rewrite
    using:
      - lem1: x = y * (1 :: int)
    targets:
      - the goal
    goal changes into:
      y * (1 :: int) = z
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `3` to provide the proof for the following goal.
pending proof goal:
  y * (1 :: int) = z
--------------------------------------------------
After Amend Have
--------------------------------------------------
variables:
  - z: int
  - x: int
premises:
  - h1: y = x + (0 :: int)
  - h2: z = y * (1 :: int)
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
variables:
  - y: int
goal: x = z
proof:
  - step id: 1
    thought: I don't know!!!
    operation: Have
    statement:
      english: x squared plus 1 is greater than 0
      isabelle: x = y * 2
    name: lem1
    proof:
      - step id: 1.1
        operation: Obvious
        Error: Automatic proof fails. The goal is non-trivial and a step-by-step proof is required.
    Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.1` to replace it with a proof.
    pending proof goal:
      x = y * (2 :: int)
  - step id: 2
    thought: Rewrite the premises to simplify the equations
    operation: Rewrite
    using:
      - lem1: x = y * (2 :: int)
    targets:
      - the goal
    goal changes into:
      y * (2 :: int) = z
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `3` to provide the proof for the following goal.
pending proof goal:
  y * (2 :: int) = z
