--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - k: int
  - i: int
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: i ≤ k ⟶ (2 :: int) * i ≤ i + k
proof:
  - step id: 1
    operation: Intro
    assuming premises:
      - premise0: i ≤ k
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  (2 :: int) * i ≤ i + k
--------------------------------------------------
Interaction Prompt
--------------------------------------------------
The induction rule `int_le_induct` has a schematic variable that must be instantiated before the rule can be applied.
Consume premises (they become `Prem<i>` subgoals, or are discharged by `using` facts):
  0. i \<le> ?k
Schematic variables to instantiate:
  - ?k :: int
Answer with `instantiations`, a list of {variable, term} objects. Each term must be a type-correct Isabelle expression.

Attempt 1 rejected as expected:
  Instantiation rejected by Isabelle:
Instantiation of `?k` rejected:
Type mismatch: variable `?k` expects type `int`, but term `True` has type `bool`.

--------------------------------------------------
After recovery with good instantiation
--------------------------------------------------
variables:
  - k: int
  - i: int
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: i ≤ k ⟶ (2 :: int) * i ≤ i + k
proof:
  - step id: 1
    operation: Intro
    assuming premises:
      - premise0: i ≤ k
  - step id: 2
    thought: induct on i; first try wrong type for ?k, then fix
    operation: Induction
    target term: i
    cases:
      - goal id: 2.base
        goal: i ≤ k
        proof: empty
        Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2.base.1` to provide the proof for the following goal.
      - goal id: 2.step
        goal: (2 :: int) * k ≤ k + k
        proof: empty
        Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2.step.1` to provide the proof for the following goal.
      - goal id: 2.Prem0
        premises:
          - Prem0.prem0: i ≤ k
          - Prem0.prem1: (2 :: int) * i ≤ i + k
        goal: i - (2 :: int) < k
        proof: empty
        Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2.Prem0.1` to provide the proof for the following goal.
