--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - n: nat
  - P: nat ⇒ bool
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: P n
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
After fill (nat.induct, 2 cases)
--------------------------------------------------
variables:
  - n: nat
  - P: nat ⇒ bool
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: P n
proof:
  - step id: 1
    thought: default induction on n (nat.induct, 2 cases: 0, Suc)
    operation: Induction
    target term: n
    Error: The variable P appear in the induction context but are not classified in the 'variables' argument. You should indicate whether each is 'arbitrary' (generalized during induction) or 'fixed' (held constant).
    cases:
      - goal id: 1.0
        goal: P (0 :: nat)
        Error: the evaluation is cancelled due to failures in preceding nodes
        proof: empty
        Error: Evaluation cancelled due to failures above
      - goal id: 1.Suc
        goal: unknown, evaluation pending
        Error: the evaluation is cancelled due to failures in preceding nodes
        proof: empty
        Error: Evaluation cancelled due to failures above
--------------------------------------------------
After amend (less_induct, 1 case) — show_warnings=True
--------------------------------------------------
variables:
  - n: nat
  - P: nat ⇒ bool
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: P n
proof:
  - step id: 1
    thought: amend to strong induction (less_induct, 1 case)
    operation: Induction
    target term: n
    fixing variables:
      - n: nat
    assuming premises:
      - less.IH: ?y < n ⟶ P ?y
    notice:
      - The variable P are not classified as fixed or generalized; fixed is assumed. Change this by calling the `edit` tool with action `amend` and target step `1`
    cases: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
pending proof goal:
  P n
