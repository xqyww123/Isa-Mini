--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - y: nat
  - q: nat
  - x: nat
premises:
  - h1: x mod q = y mod q
  - h2: coprime q (2 :: nat)
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: x mod q = y mod q
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
Response
--------------------------------------------------
Is error: True
Reason: Cannot fill a node with id 1
the given fact h1 mismatches the target premise ‹(?n::?'a::{euclidean_ring_cancel,semiring_gcd}) * (?a::?'a::{euclidean_ring_cancel,semiring_gcd}) mod (?m::?'a::{euclidean_ring_cancel,semiring_gcd}) = ?n * (?b::?'a::{euclidean_ring_cancel,semiring_gcd}) mod ?m›
because type nat ⇒ nat ⇒ bool does not unify with ?'a::{euclidean_ring_cancel,semiring_gcd} ⇒ ?'a::{euclidean_ring_cancel,semiring_gcd} ⇒ bool
--------------------------------------------------
After Derive
--------------------------------------------------
variables:
  - y: nat
  - q: nat
  - x: nat
premises:
  - h1: x mod q = y mod q
  - h2: coprime q (2 :: nat)
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: x mod q = y mod q
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
