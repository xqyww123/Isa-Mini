--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - n: nat
  - P: nat ⇒ bool
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: P n
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof.
--------------------------------------------------
After nested induction (outer Suc body has inner Induction)
--------------------------------------------------
variables:
  - n: nat
  - P: nat ⇒ bool
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: P n
proof:
  - step id: 1
    thought: outer induction on n
    operation: Induction
    target term: n
    cases:
      - goal id: 1.0
        goal: P (0 :: nat)
        proof:
          - step id: 1.0.1
            operation: Obvious
            Error: Automatic proof fails. The goal is non-trivial and a step-by-step proof is required.
        Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.0.1` to replace it with a proof.
        pending proof goal:
          P (0 :: nat)
      - goal id: 1.Suc
        premises:
          - Suc.IH: P n
        goal: P (Suc n)
        proof:
          - step id: 1.Suc.1
            thought: inner induction on the same variable n
            operation: Induction
            target term: n
            cases:
              - goal id: 1.Suc.1.0
                goal: P (Suc (0 :: nat))
                proof:
                  - step id: 1.Suc.1.0.1
                    operation: Obvious
                    Error: Automatic proof fails. The goal is non-trivial and a step-by-step proof is required.
                Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.Suc.1.0.1` to replace it with a proof.
                pending proof goal:
                  P (Suc (0 :: nat))
              - goal id: 1.Suc.1.Suc1
                premises:
                  - Suc1.IH: P (Suc n)
                goal: P (Suc (Suc n))
                proof:
                  - step id: 1.Suc.1.Suc1.1
                    operation: Obvious
                    Error: Automatic proof fails. The goal is non-trivial and a step-by-step proof is required.
                Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.Suc.1.Suc1.1` to replace it with a proof.
                pending proof goal:
                  P (Suc (Suc n))

MapCase interactions fired: 0 (expected 0 — fuzzy match handles `Suc1`→`Suc`)
