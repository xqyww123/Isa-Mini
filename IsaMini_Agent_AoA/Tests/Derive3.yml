--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - P: nat ⇒ bool
premises:
  - h1: P (0 :: nat)
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: P (0 :: nat)
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof for the following goal.
--------------------------------------------------
Response
--------------------------------------------------
Is error: True
Reason: FailureReason(reason='Rule fact "nonexistent_rule" not found\n')
--------------------------------------------------
After Derive (unfound)
--------------------------------------------------
variables:
  - P: nat ⇒ bool
premises:
  - h1: P (0 :: nat)
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: P (0 :: nat)
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof for the following goal.
