--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - Q: nat ⇒ bool
premises:
  - h1: P (0 :: nat)
  - h2: ∀(x :: nat). P x ⟶ Q x
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
variables:
  - P: nat ⇒ bool
goal: Q (0 :: nat)
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof for the following goal.
--------------------------------------------------
After Derive
--------------------------------------------------
variables:
  - Q: nat ⇒ bool
premises:
  - h1: P (0 :: nat)
  - h2: ∀(x :: nat). P x ⟶ Q x
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
variables:
  - P: nat ⇒ bool
goal: Q (0 :: nat)
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof for the following goal.
No step 2 needed (auto-proved after Derive): CannotFill_BadNode: Cannot fill a node with id 2
Unfinished nodes: 1
