--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - x: int
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: (0 :: int) ≤ x * x
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof for the following goal.
is_error: False
--------------------------------------------------
After batched fill [Have(proof:[Obvious]), Obvious]
--------------------------------------------------
variables:
  - x: int
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: (0 :: int) ≤ x * x
proof:
  - step id: 1
    thought: restate goal as aux
    operation: Have
    statement:
      english: x*x nonneg
      isabelle: x * x \<ge> 0
    name: aux
    proof:
      - step id: 1.1
        operation: Obvious
  - step id: 2
    operation: Obvious
    with facts:
      - aux: (0 :: int) ≤ x * x
Unfinished nodes: 0
