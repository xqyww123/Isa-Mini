--------------------------------------------------
Initial YAML
--------------------------------------------------
variables:
  - x: int
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: (0 :: int) ≤ x * x
proof: empty
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1` to provide the proof for the following goal.
--------------------------------------------------
After Have x=x (Obvious succeeds)
--------------------------------------------------
variables:
  - x: int
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: (0 :: int) ≤ x * x
proof:
  - step id: 1
    thought: trivial identity
    operation: Have
    statement:
      english: x equals x
      isabelle: x = x
    name: lem1
    proof: empty
    Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.1` to provide the proof for the following goal.
    pending proof goal:
      x = x
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2` to provide the proof for the following goal.
--------------------------------------------------
After Have x=x+1 (Obvious fails)
--------------------------------------------------
variables:
  - x: int
You can write global declarations by calling command `edit` with action `fill` and target step `global.1`
goal: (0 :: int) ≤ x * x
proof:
  - step id: 1
    thought: trivial identity
    operation: Have
    statement:
      english: x equals x
      isabelle: x = x
    name: lem1
    proof: empty
    Error: Unfinished Proof! Call command `edit` with action `fill` and target step `1.1` to provide the proof for the following goal.
    pending proof goal:
      x = x
  - step id: 2
    thought: this is false
    operation: Have
    statement:
      english: x equals x plus one
      isabelle: x = x + 1
    name: bad
    proof: empty
    Error: Unfinished Proof! Call command `edit` with action `fill` and target step `2.1` to provide the proof for the following goal.
    pending proof goal:
      x = x + (1 :: int)
Error: Unfinished Proof! Call command `edit` with action `fill` and target step `3` to provide the proof for the following goal.
--------------------------------------------------
Overview
--------------------------------------------------
step 1: Have lem1 (line 0)
  Error: Unfinished Proof (line 46)
  goal: x = x
step 2: Have bad (line 0)
  Error: Unfinished Proof (line 57)
  goal: x = x + (1 :: int)
Error: Unfinished Proof (line 60)
goal: (0 :: int) ≤ x * x
Unfinished nodes: 3
